======================================================================
BUNNYDL INLINE FLOW INSPECTOR
======================================================================
STATUS: 200
URL: https://dlbunny.com/id/bilibili
HTML SIZE: 189202 bytes

======================================================================
TARGET FUNCTIONS / VARIABLES
======================================================================

######################################################################
TARGET: create_oxy_order
OCCURRENCES: 1
######################################################################

--- OCCURRENCE 1 (offset 136379) ---
he maximum text limit is exceeded, Change a voice, or reduce the length of the text')

                $("#UserInputTextarea").css('border-color', 'red');
                $("#UserInputTextarea").css('background-color', '#ffe6e6');
                order_is_running = 0;
                return 0
            } else {
                $("#UserInputTextarea").css('border-color', '');
                $("#UserInputTextarea").css('background-color', '');
            }

            let user_anonymous = $("#userSelectAnonymousMode").checked;
            let user_input_captcha_text = $("#UserInputCaptcha").val();

            if (!user_input_captcha_text) {
                showWarnMsgPrompt(messages2);

                $("#UserInputCaptcha").focus();
                $("#UserInputCaptcha").css('border', '1px solid red');
                $("#UserInputCaptcha").css('background-color', '#ffcccc');
                order_is_running = 0;
                return 0
            } else {
                $("#UserInputCaptcha").css('border', '1px solid #ced4da');
                $("#UserInputCaptcha").css('background-color', '#fff');
            }


            let user_uuid_text = $("#VerifyCaptchaIMG").attr('uuid');
            if (!user_uuid_text) {
                toastr.warning('Unable to obtain UUID, please try again or change browser')
                return 0
            }

            let user_select_announcer_is_need_queue = $("input[name='RadioUserSelectAppID']:checked").attr('data-is-need-queue');
            console.log(user_select_announcer_is_need_queue);

            if (user_select_announcer_is_need_queue == 'true') {
                console.log('need queue')



            } else {
                console.log('not need queue')

            }

            let targetUrl = '\/api\/create_oxy_order'
            let dataJSON = {}
            let uuid = getCookieValue("uuid");
            if (uuid != "") {
                user_uuid_text = uuid;
                console.log("uuid:"+user_uuid_text);
            }
            dataJSON['user_uuid_text'] = user_uuid_text
            dataJSON['user_input_text'] = user_input_text_str
            
            
            dataJSON['user_select_media_format'] = user_select_setting_audio_format
            dataJSON['user_select_appid'] = Number(user_select_app_id)
            dataJSON['user_select_app'] = user_select_app
            dataJSON['user_input_captcha_text'] = user_input_captcha_text
            dataJSON['user_anonymous'] = user_anonymous

            
            

            
            if (dataJSON['user_input_text'].indexOf('??') != -1) {
                for (let i = 0; i < 100; i++) {
                    dataJSON['user_input_text'] = dataJSON['user_input_text'].replace('??', '?')
                }
            }

            disable_post_btn();
            progress_control_start(id = user_select_app_id, is_need_queue = user_select_announcer_is_need_queue)

            let timeout_setting = 200000
            if (user_input_text_str.length > 20000) {
                timeout_setting = 400000
            }

            $.ajax({
                type: 'post',
                url: targetUrl,
                data: JSON.stringify(dataJSON),
                dataType: 'json',

                timeout: timeout_setting,
                success: function (data) {
                    
                    if (data.status == 500) {
                        toastr.options.timeOut = 30000;
                        toastr.options.positionClass = 'toast-top-right';
                        toastr.error(data.info);
                        toastr.options.timeOut = 5000;
                        $('#reVerify').click();
                        $("#UserInputCaptcha").val('');
                        enable_post_btn();
                        progress_control_stop()



                    } else if (data.status == 403) {
                        
                        toastr.warning(data.info);
                        enable_post_btn();
                        $('#reVerify').click();
                        $("#UserInputCaptcha").val('');
                        progress_control_stop()



                    } else if (data.status == 404) {
                        
                        toastr.warning(data.info);
                        enable_post_btn();
                        $('#reVerify').click();
                        progress_control_stop()



                    } else if (data.status == 407) {
                        
                        toastr.warning(data.info);
                        enable_post_btn();
                        $('#reVerify').click();
                        $("#UserInputCaptcha").val('');
                        progress_control_stop();
                        $("#UserInputTextarea").css('border-color', 'red');
                        $("#UserInputTextarea").css('background-color', '#ffe6e6');

                    } else if (data.status == 409) {
                        
                        enable_post_btn();
                        $('#reVerify').click();
                        progress_control_stop()
                        $("#UserInputCaptcha").val('');
                        $("#UserInputCaptcha").focus();
                        $("#UserInpu

######################################################################
TARGET: submitOxyerForm
OCCURRENCES: 1
######################################################################

--- OCCURRENCE 1 (offset 133352) ---
veAttr('disabled');
            $("#order_more_setting").removeAttr('disabled');
            order_is_running = 0;
        }

        function enable_post_container() {
            $("#download_player_div").removeAttr('hidden');
            $("#download_player_div").show();
            $('html,body').animate({scrollTop: $("#download_player_div").offset().top - 10}, 800)
        }

        function show_download_btn() {
            $("#download_btn").removeAttr('hidden');
            $("#download_btn").show();
            $("#buy_coffee").hide(); 
        }

        function show_video_download_btn(data) {
            $("#download_btn").hide();
            create_video_buttons(data);
            create_cover_button(data.video_cover);
            create_subs_buttons(data);
        }

        function bindDownloadURL(addr, fileType, title) {
            const downloadBtn = document.getElementById('download_btn');
            const newBtn = downloadBtn.cloneNode(true);
            downloadBtn.parentNode.replaceChild(newBtn, downloadBtn);
            newBtn.addEventListener('click', function() {
                downloadFileProV2(
                    addr,
                    fileType,
                    title
                );
            });
        }

        function play_and_pause_mp3() {
            var audioEle = document.getElementById("download_player_audio");
            audioEle.load();

            window.wavesurfer.on('ready', function () {
                console.log('音频加载成功，WaveSurfer 准备就绪');
                if (audioEle.paused) {
                    audioEle.play();
                } else {
                    audioEle.pause();
                }
            });
        }

        var current_download_url_for_play = '';
        var post_to_backend = function submitOxyerForm() {
            order_is_running = 1;
            var user_input_text_str = $("#UserInputTextarea").val();
            user_input_text_str = user_input_text_str.trim()
            if (!user_input_text_str) {
                showWarnMsgPrompt(messages1);
                $("#UserInputTextarea").css('border', '1px solid red');
                return 0
            }

            
            let user_select_app = $("input[name='RadioUserSelectAppID']:checked").attr('data-app-id');
            let user_select_app_id = $("input[name='RadioUserSelectAppID']:checked").val();
            user_select_app = "bilibili";
            user_select_app_id = 3;
            if (!user_select_app_id) {
                toastr.warning('Please choose the app you like')
                return 0
            }

            var user_select_setting_audio_format = $("input[name='RadioUserSelectMediaFormat']:checked").val();
            let user_select_announcer_input_text_limit_count = $("input[name='RadioUserSelectAppID']:checked").attr('data-limit-text-count');

            if (user_input_text_str.length > user_select_announcer_input_text_limit_count) {
                toastr.warning('The text you entered is too long, The maximum text limit is exceeded, Change a voice, or reduce the length of the text')

                $("#UserInputTextarea").css('border-color', 'red');
                $("#UserInputTextarea").css('background-color', '#ffe6e6');
                order_is_running = 0;
                return 0
            } else {
                $("#UserInputTextarea").css('border-color', '');
                $("#UserInputTextarea").css('background-color', '');
            }

            let user_anonymous = $("#userSelectAnonymousMode").checked;
            let user_input_captcha_text = $("#UserInputCaptcha").val();

            if (!user_input_captcha_text) {
                showWarnMsgPrompt(messages2);

                $("#UserInputCaptcha").focus();
                $("#UserInputCaptcha").css('border', '1px solid red');
                $("#UserInputCaptcha").css('background-color', '#ffcccc');
                order_is_running = 0;
                return 0
            } else {
                $("#UserInputCaptcha").css('border', '1px solid #ced4da');
                $("#UserInputCaptcha").css('background-color', '#fff');
            }


            let user_uuid_text = $("#VerifyCaptchaIMG").attr('uuid');
            if (!user_uuid_text) {
                toastr.warning('Unable to obtain UUID, please try again or change browser')
                return 0
            }

            let user_select_announcer_is_need_queue = $("input[name='RadioUserSelectAppID']:checked").attr('data-is-need-queue');
            console.log(user_select_announcer_is_need_queue);

            if (user_select_announcer_is_need_queue == 'true') {
                console.log('need queue')



            } else {
                console.log('not need queue')

            }

            let targetUrl = '\/api\/create_oxy_order'
            let dataJSON = {}
            let uuid = getCookieValue("uuid");
            if (uuid != "") {
                user_uuid_text = uuid;
                console.log("uuid:"+user_uuid_text);
            }
            dataJSON['user_uuid_text'] = user_uuid_text
            dataJSON['user_input_text'] = user_input_text_str
            
            
            dataJSON['user_select_media_format'] = user_select_setting_audio_format
            da

######################################################################
TARGET: bindDownloadURL
OCCURRENCES: 3
######################################################################

--- OCCURRENCE 1 (offset 132362) ---
);
            $('#jprogress_tips_div').hide();

            if (all_progress_bar) {
                reset_progress_to_0_status();
            }
        }
    </script>

<script type="text/javascript">
        var order_is_running = 0;

        function getCookieValue(name) {
            
            var cookieArr = document.cookie.split(";");
            
            for(var i = 0; i < cookieArr.length; i++) {
                var cookiePair = cookieArr[i].split("=");

                

                if(name == cookiePair[0].trim()) {
                    
                    return decodeURIComponent(cookiePair[1]);
                }
            }

            
            return "";
        }

        function disable_post_btn() {
            $("#order_submit").attr('disabled', 'disabled');
            $("#order_more_setting").attr('disabled', 'disabled');
            order_is_running = 1;
        };

        function enable_post_btn() {
            $("#order_submit").removeAttr('disabled');
            $("#order_more_setting").removeAttr('disabled');
            order_is_running = 0;
        }

        function enable_post_container() {
            $("#download_player_div").removeAttr('hidden');
            $("#download_player_div").show();
            $('html,body').animate({scrollTop: $("#download_player_div").offset().top - 10}, 800)
        }

        function show_download_btn() {
            $("#download_btn").removeAttr('hidden');
            $("#download_btn").show();
            $("#buy_coffee").hide(); 
        }

        function show_video_download_btn(data) {
            $("#download_btn").hide();
            create_video_buttons(data);
            create_cover_button(data.video_cover);
            create_subs_buttons(data);
        }

        function bindDownloadURL(addr, fileType, title) {
            const downloadBtn = document.getElementById('download_btn');
            const newBtn = downloadBtn.cloneNode(true);
            downloadBtn.parentNode.replaceChild(newBtn, downloadBtn);
            newBtn.addEventListener('click', function() {
                downloadFileProV2(
                    addr,
                    fileType,
                    title
                );
            });
        }

        function play_and_pause_mp3() {
            var audioEle = document.getElementById("download_player_audio");
            audioEle.load();

            window.wavesurfer.on('ready', function () {
                console.log('音频加载成功，WaveSurfer 准备就绪');
                if (audioEle.paused) {
                    audioEle.play();
                } else {
                    audioEle.pause();
                }
            });
        }

        var current_download_url_for_play = '';
        var post_to_backend = function submitOxyerForm() {
            order_is_running = 1;
            var user_input_text_str = $("#UserInputTextarea").val();
            user_input_text_str = user_input_text_str.trim()
            if (!user_input_text_str) {
                showWarnMsgPrompt(messages1);
                $("#UserInputTextarea").css('border', '1px solid red');
                return 0
            }

            
            let user_select_app = $("input[name='RadioUserSelectAppID']:checked").attr('data-app-id');
            let user_select_app_id = $("input[name='RadioUserSelectAppID']:checked").val();
            user_select_app = "bilibili";
            user_select_app_id = 3;
            if (!user_select_app_id) {
                toastr.warning('Please choose the app you like')
                return 0
            }

            var user_select_setting_audio_format = $("input[name='RadioUserSelectMediaFormat']:checked").val();
            let user_select_announcer_input_text_limit_count = $("input[name='RadioUserSelectAppID']:checked").attr('data-limit-text-count');

            if (user_input_text_str.length > user_select_announcer_input_text_limit_count) {
                toastr.warning('The text you entered is too long, The maximum text limit is exceeded, Change a voice, or reduce the length of the text')

                $("#UserInputTextarea").css('border-color', 'red');
                $("#UserInputTextarea").css('background-color', '#ffe6e6');
                order_is_running = 0;
                return 0
            } else {
                $("#UserInputTextarea").css('border-color', '');
                $("#UserInputTextarea").css('background-color', '');
            }

            let user_anonymous = $("#userSelectAnonymousMode").checked;
            let user_input_captcha_text = $("#UserInputCaptcha").val();

            if (!user_input_captcha_text) {
                showWarnMsgPrompt(messages2);

                $("#UserInputCaptcha").focus();
                $("#UserInputCaptcha").css('border', '1px solid red');
                $("#UserInputCaptcha").css('background-color', '#ffcccc');
                order_is_running = 0;
                return 0
            } else {
                $("#UserInputCaptcha").css('border', '1px solid #ced4da');
                $("#UserInputCaptcha").css('background-color', '#fff');
            }


            let user_uuid_text = $("#VerifyCaptchaIMG").attr('uuid');
            if (!user_uuid_text) {
                toastr.warning('Unabl

--- OCCURRENCE 2 (offset 167842) ---
d_player_anim_panel").hide();
           $("#dl_wrapper").removeClass('display-none');
           initFluidPlayer(data.video_base_url, data.video_cover);
           try {
               var task_videoUrl = data.video_base_url;
               var task_text = data.media_desc;
               var task_validityPeriod = 30;
               var task_appName = data.post_data.user_select_app;
               var task_mediaFormat = data.media_format;

               THPaddHistory(task_videoUrl, task_text, task_validityPeriod, task_appName, task_mediaFormat);
           } catch (e) {
               console.log("THPaddHistory exception:"+e)
           }
       }
   }

   function show_audio_div(data) {
       if (data.media_format === "mp3") {

           if (!data.auto_mute_share && data.auto_stand_url === "") {
               $("#download_btn").prop('disabled', true);
               setTimeout(toastr.warning, 3000, data.auto_offline_desc);
           }

           show_download_btn();
           injectAd(data);
           try {
               var this_task_return_mp3_url = data.auto_stand_url
               window.wavesurfer.load(data.auto_stand_url)
               window.wavesurfer.saveurl = data.auto_stand_url
           } catch (e) {
               console.log(e)
           }

           $("#download_player_anim_panel").hide();
           $("#waveform").removeClass("display-none")
           $("#download_player_audio").removeClass("display-none")
           current_download_url_for_play = data.auto_stand_url
           $("#download_player_audio").attr('src', data.auto_stand_url);
           $("#download_player_source").attr('src', data.auto_stand_url);
           $("#download_player_source").attr('type', "audio/" + data.audio_format);
           play_and_pause_mp3();
           bindDownloadURL(data.auto_stand_url, data.media_format, data.media_desc);
           create_afd_button();
           try {
               var task_audioUrl = data.auto_stand_url;
               var task_text = data.media_desc;
               var task_validityPeriod = 30;
               var task_appName = data.post_data.user_select_app;
               var task_mediaFormat = data.media_format;

               if (task_audioUrl.length > 0) {
                   THPaddHistory(task_audioUrl, task_text, task_validityPeriod, task_appName, task_mediaFormat);
               }
           } catch (e) {
               console.log("THPaddHistory exception:"+e)
           }
       }
   }

   function show_images_div(data) {
       if (data.media_format === "webp" || data.media_format === "jpeg") {
           if (data.media_type != 2 || data.image_url_list.length == 0) {
               let datatemp = JSON.parse(JSON.stringify(data));
               datatemp.media_format = "mp4"
               setTimeout(toastr.warning, 3000, "检测到包含视频资源，已为您切换至视频");
               show_video_div(datatemp);
               return;
           }

           show_download_btn();
           console.info("data.image_url_list:"+data.image_url_list);
           $("#download_player_anim_panel").hide();
           $("#gallery").show();
           const gallery = document.getElementById('gallery');
           gallery.innerHTML = '';
           data.image_url_list.forEach(image => {
               
               const itemHTML = `
      <div class="grid-item">
        <a href="${image}" data-lightbox="gallery" data-title="${image}">
          <img src="${image}" alt="${image}" style="width: 100%; height: auto;">
        </a>
      </div>
    `;
               gallery.insertAdjacentHTML('beforeend', itemHTML);
           });

           
           bindDownloadURL('', data.media_format, data.media_desc);
           create_afd_button();
           try {
               var task_imagesUrl = data.image_url_list;
               var task_text = data.media_desc;
               var task_validityPeriod = 30;
               var task_appName = data.post_data.user_select_app;
               var task_mediaFormat = data.media_format;

               THPaddHistory(task_imagesUrl, task_text, task_validityPeriod, task_appName, task_mediaFormat);
           } catch (e) {
               console.log("THPaddHistory exception:"+e)
           }
       }
   }

    function create_cover_button(cover) {
        const url = cover
        if (url.length === 0) {
            return;
        }
        const screenWidth = window.innerWidth;
        var container = document.getElementById('buttons-container');
        var button = document.createElement('button');
        button.type = 'button';
        
        button.className = 'btn btn-lg btn accent display-block col-3-5';
        if (screenWidth <= 768) {
            button.className = 'btn btn-lg btn accent display-block col-11';
        }
        button.id = 'button-cover';
        button.style= 'margin-left: 10px;';
        let Id = button.id
        let one_click_text = `downloadVideoList('${Id}','${url}','webp','cover')`

        button.innerText = "🏔️ Sampul video";
        button.setAttribute('onclick', one_click_text);
        container.appendChild(button);
    }

    function create_subs_buttons(data) {
        const screenWidth = window.innerWidth;
        var container = document.getElementById('buttons-container');
        for (var i = 0; i < data.

--- OCCURRENCE 3 (offset 169675) ---
rl, data.media_format, data.media_desc);
           create_afd_button();
           try {
               var task_audioUrl = data.auto_stand_url;
               var task_text = data.media_desc;
               var task_validityPeriod = 30;
               var task_appName = data.post_data.user_select_app;
               var task_mediaFormat = data.media_format;

               if (task_audioUrl.length > 0) {
                   THPaddHistory(task_audioUrl, task_text, task_validityPeriod, task_appName, task_mediaFormat);
               }
           } catch (e) {
               console.log("THPaddHistory exception:"+e)
           }
       }
   }

   function show_images_div(data) {
       if (data.media_format === "webp" || data.media_format === "jpeg") {
           if (data.media_type != 2 || data.image_url_list.length == 0) {
               let datatemp = JSON.parse(JSON.stringify(data));
               datatemp.media_format = "mp4"
               setTimeout(toastr.warning, 3000, "检测到包含视频资源，已为您切换至视频");
               show_video_div(datatemp);
               return;
           }

           show_download_btn();
           console.info("data.image_url_list:"+data.image_url_list);
           $("#download_player_anim_panel").hide();
           $("#gallery").show();
           const gallery = document.getElementById('gallery');
           gallery.innerHTML = '';
           data.image_url_list.forEach(image => {
               
               const itemHTML = `
      <div class="grid-item">
        <a href="${image}" data-lightbox="gallery" data-title="${image}">
          <img src="${image}" alt="${image}" style="width: 100%; height: auto;">
        </a>
      </div>
    `;
               gallery.insertAdjacentHTML('beforeend', itemHTML);
           });

           
           bindDownloadURL('', data.media_format, data.media_desc);
           create_afd_button();
           try {
               var task_imagesUrl = data.image_url_list;
               var task_text = data.media_desc;
               var task_validityPeriod = 30;
               var task_appName = data.post_data.user_select_app;
               var task_mediaFormat = data.media_format;

               THPaddHistory(task_imagesUrl, task_text, task_validityPeriod, task_appName, task_mediaFormat);
           } catch (e) {
               console.log("THPaddHistory exception:"+e)
           }
       }
   }

    function create_cover_button(cover) {
        const url = cover
        if (url.length === 0) {
            return;
        }
        const screenWidth = window.innerWidth;
        var container = document.getElementById('buttons-container');
        var button = document.createElement('button');
        button.type = 'button';
        
        button.className = 'btn btn-lg btn accent display-block col-3-5';
        if (screenWidth <= 768) {
            button.className = 'btn btn-lg btn accent display-block col-11';
        }
        button.id = 'button-cover';
        button.style= 'margin-left: 10px;';
        let Id = button.id
        let one_click_text = `downloadVideoList('${Id}','${url}','webp','cover')`

        button.innerText = "🏔️ Sampul video";
        button.setAttribute('onclick', one_click_text);
        container.appendChild(button);
    }

    function create_subs_buttons(data) {
        const screenWidth = window.innerWidth;
        var container = document.getElementById('buttons-container');
        for (var i = 0; i < data.subs.length; i++) {
            (function(index) {
                let ext = data.subs[i].ext;
                let url = data.subs[i].url;
                let name = data.subs[i].name;
                let donate_flag = data.subs[i].donate_flag;
                let fname = data.media_desc;

                var button = document.createElement('button');
                button.type = 'button';
                
                button.className = 'btn btn-lg btn gray display-block col-3-5';
                if (screenWidth <= 768) {
                    button.className = 'btn btn-lg btn gray display-block col-11';
                }
                button.id = 'button-sub-' + i;
                button.style= 'margin-left: 10px;';
                let Id = button.id

                let btnText = "Subtitle";
                btnText += ` @`+ ext +` - ` + name;
                button.innerText = btnText;
                button.addEventListener('click', function() {
                    downloadVideoList(
                        Id,
                        url,
                        ext,
                        fname
                    );
                });
                if (donate_flag) {
                    button.className += " donate-icon-button tooltip-button";
                }
                if (url.length === 0) {
                    button.disabled = true;
                }
                container.appendChild(button);
            })(i);
        }
    }

   function show_author_div(data) {
       $("#user_card").removeClass("display-none");
       $("#video_author_head").css('background-image', 'url("' + data.author_head + '")');
       $("#video_author_nick").text(data.author_name);
       $("#video_author_art").text(data.media_desc);
   }
</script>
<script>
    
    var listItemHeight;
    functi

######################################################################
TARGET: downloadVideoList
OCCURRENCES: 4
######################################################################

--- OCCURRENCE 1 (offset 160823) ---
me = 'btn btn-lg btn accent display-block col-3-5';
                if (screenWidth <= 768) {
                    button.className = 'btn btn-lg btn accent display-block col-11';
                }
                button.id = 'button-' + i;
                button.style= 'margin-left: 10px;';
                let Id = button.id
                let format = 'mp4';
                let title = data.media_desc;
                

                let btnText = "Unduh";
                if (data.video_play_addr[i].width > 0 || data.video_play_addr[i].height > 0) {
                    btnText += ` [` + data.video_play_addr[i].width + `x` + data.video_play_addr[i].height + `] `;
                } else if (data.video_play_addr[i].ext === "flv") {
                    
                    btnText += ` Live「` + data.video_play_addr[i].audio_quality +`」`;
                    format = 'flv';
                } else {
                    
                    btnText += `「🎵mp3」`;
                    format = 'mp3';
                }

                if (data.video_play_addr[i].attr === "backup") {
                    btnText += ` 📥 @Backup`
                } else if (file_size !== '0.00M') {
                    btnText += file_size;
                }
                button.innerText = btnText;

                if (data.video_play_addr[i].donate_flag) {
                    $("#buy_coffee").removeAttr("hidden");
                    $("#buy_coffee").show();
                    button.className += " donate-icon-button tooltip-button";
                }
                if (url.length === 0) {
                    button.disabled = true;
                    change_afdx_button(button);
                } else {
                    button.addEventListener('click', function() {
                        downloadVideoList(
                            Id,
                            url,
                            format,
                            title
                        );
                    });
                }

                container.appendChild(button);
            })(i);
        }
    }

    function downloadVideoList(id, url, mediaType, title) {
        if (mediaType === "flv") {
            window.open(url, '_blank');
            return;
        }

        let el = $(`#${id}`);
        let download_btn_old_onclick = el.attr('onclick');
        el.attr('onclick', '');
        el.css('display', 'inline');
        el.prop('disabled', true);

        try {
            $("#download_progress_div").removeClass('display-none');
        } catch (e) {
            console.log(e)
        }

        let download_btn_old_text = el.text();
        let download_show_percent_text = '0%';
        let download_show_full_text = download_show_percent_text + ' Downloading...';
        el.text(download_show_full_text);
        var x = new XMLHttpRequest();
        x.open("GET", url, true);

        x.responseType = 'blob';
        x.addEventListener('progress', function (event) {
            if (event.lengthComputable) {
                const percentComplete = Math.round((event.loaded / event.total) * 100);

                let file_size = (event.total / 1024 / 1024).toFixed(2) + 'M';
                let task_full_text = percentComplete + '% Downloading (File size: ' + file_size + ')...';
                el.text(task_full_text);
                try {
                    $("#download_progress_bar").css('width', percentComplete + '%');
                } catch (e) {
                    console.log(e)
                }
            }
        });
        x.onload = function (e) {
            if (x.status === 403) {
                window.open(x.responseURL, '_blank');
                return;
            }

            var url = window.URL.createObjectURL(x.response);
            var a = document.createElement('a');
            a.href = url;

            let file_type = mediaType;
            let current_date = new Date();
            let year = current_date.getFullYear();
            let month = current_date.getMonth() + 1;
            let day = current_date.getDate();
            let hour = current_date.getHours();
            let minute = current_date.getMinutes();
            let second = current_date.getSeconds();
            let data_str = year + '-' + month + '-' + day + '-' + hour + '-' + minute + '-' + second;
            let file_name = '';
            if (title === "") {
                file_name = data_str;
            } else {
                file_name = title;
            }
            let full_name = '[DLBunny.com][bilibili]' + file_name + '.' + file_type;
            a.download = full_name;
            a.click();

            toastr.success("The file has been downloaded successfully. File Name: " + full_name);

            el.removeAttr('disabled');
            el.text(download_btn_old_text);
            el.css('display', 'inline');
            el.prop('disabled', false);
            el.attr('onclick', download_btn_old_onclick);

            try {

                $("#download_progress_div").addClass('display-none');
                $("#download_progress_bar").css('width', '0%');

            } catch (e) {
                console.log(e)
            }
        }

        x.onerror = function() {
            console.error('An error o

--- OCCURRENCE 2 (offset 161143) ---
ton.id
                let format = 'mp4';
                let title = data.media_desc;
                

                let btnText = "Unduh";
                if (data.video_play_addr[i].width > 0 || data.video_play_addr[i].height > 0) {
                    btnText += ` [` + data.video_play_addr[i].width + `x` + data.video_play_addr[i].height + `] `;
                } else if (data.video_play_addr[i].ext === "flv") {
                    
                    btnText += ` Live「` + data.video_play_addr[i].audio_quality +`」`;
                    format = 'flv';
                } else {
                    
                    btnText += `「🎵mp3」`;
                    format = 'mp3';
                }

                if (data.video_play_addr[i].attr === "backup") {
                    btnText += ` 📥 @Backup`
                } else if (file_size !== '0.00M') {
                    btnText += file_size;
                }
                button.innerText = btnText;

                if (data.video_play_addr[i].donate_flag) {
                    $("#buy_coffee").removeAttr("hidden");
                    $("#buy_coffee").show();
                    button.className += " donate-icon-button tooltip-button";
                }
                if (url.length === 0) {
                    button.disabled = true;
                    change_afdx_button(button);
                } else {
                    button.addEventListener('click', function() {
                        downloadVideoList(
                            Id,
                            url,
                            format,
                            title
                        );
                    });
                }

                container.appendChild(button);
            })(i);
        }
    }

    function downloadVideoList(id, url, mediaType, title) {
        if (mediaType === "flv") {
            window.open(url, '_blank');
            return;
        }

        let el = $(`#${id}`);
        let download_btn_old_onclick = el.attr('onclick');
        el.attr('onclick', '');
        el.css('display', 'inline');
        el.prop('disabled', true);

        try {
            $("#download_progress_div").removeClass('display-none');
        } catch (e) {
            console.log(e)
        }

        let download_btn_old_text = el.text();
        let download_show_percent_text = '0%';
        let download_show_full_text = download_show_percent_text + ' Downloading...';
        el.text(download_show_full_text);
        var x = new XMLHttpRequest();
        x.open("GET", url, true);

        x.responseType = 'blob';
        x.addEventListener('progress', function (event) {
            if (event.lengthComputable) {
                const percentComplete = Math.round((event.loaded / event.total) * 100);

                let file_size = (event.total / 1024 / 1024).toFixed(2) + 'M';
                let task_full_text = percentComplete + '% Downloading (File size: ' + file_size + ')...';
                el.text(task_full_text);
                try {
                    $("#download_progress_bar").css('width', percentComplete + '%');
                } catch (e) {
                    console.log(e)
                }
            }
        });
        x.onload = function (e) {
            if (x.status === 403) {
                window.open(x.responseURL, '_blank');
                return;
            }

            var url = window.URL.createObjectURL(x.response);
            var a = document.createElement('a');
            a.href = url;

            let file_type = mediaType;
            let current_date = new Date();
            let year = current_date.getFullYear();
            let month = current_date.getMonth() + 1;
            let day = current_date.getDate();
            let hour = current_date.getHours();
            let minute = current_date.getMinutes();
            let second = current_date.getSeconds();
            let data_str = year + '-' + month + '-' + day + '-' + hour + '-' + minute + '-' + second;
            let file_name = '';
            if (title === "") {
                file_name = data_str;
            } else {
                file_name = title;
            }
            let full_name = '[DLBunny.com][bilibili]' + file_name + '.' + file_type;
            a.download = full_name;
            a.click();

            toastr.success("The file has been downloaded successfully. File Name: " + full_name);

            el.removeAttr('disabled');
            el.text(download_btn_old_text);
            el.css('display', 'inline');
            el.prop('disabled', false);
            el.attr('onclick', download_btn_old_onclick);

            try {

                $("#download_progress_div").addClass('display-none');
                $("#download_progress_bar").css('width', '0%');

            } catch (e) {
                console.log(e)
            }
        }

        x.onerror = function() {
            console.error('An error occurred during the request.');
            window.open(url, '_blank');
        };

        x.ontimeout = function() {
            console.error('An error occurred during the request. (timeout)');
            window.open(url, '_blank');
        };

        x.send();

        setTimeout(function () {
            el.remov

--- OCCURRENCE 3 (offset 170950) ---
();
           const gallery = document.getElementById('gallery');
           gallery.innerHTML = '';
           data.image_url_list.forEach(image => {
               
               const itemHTML = `
      <div class="grid-item">
        <a href="${image}" data-lightbox="gallery" data-title="${image}">
          <img src="${image}" alt="${image}" style="width: 100%; height: auto;">
        </a>
      </div>
    `;
               gallery.insertAdjacentHTML('beforeend', itemHTML);
           });

           
           bindDownloadURL('', data.media_format, data.media_desc);
           create_afd_button();
           try {
               var task_imagesUrl = data.image_url_list;
               var task_text = data.media_desc;
               var task_validityPeriod = 30;
               var task_appName = data.post_data.user_select_app;
               var task_mediaFormat = data.media_format;

               THPaddHistory(task_imagesUrl, task_text, task_validityPeriod, task_appName, task_mediaFormat);
           } catch (e) {
               console.log("THPaddHistory exception:"+e)
           }
       }
   }

    function create_cover_button(cover) {
        const url = cover
        if (url.length === 0) {
            return;
        }
        const screenWidth = window.innerWidth;
        var container = document.getElementById('buttons-container');
        var button = document.createElement('button');
        button.type = 'button';
        
        button.className = 'btn btn-lg btn accent display-block col-3-5';
        if (screenWidth <= 768) {
            button.className = 'btn btn-lg btn accent display-block col-11';
        }
        button.id = 'button-cover';
        button.style= 'margin-left: 10px;';
        let Id = button.id
        let one_click_text = `downloadVideoList('${Id}','${url}','webp','cover')`

        button.innerText = "🏔️ Sampul video";
        button.setAttribute('onclick', one_click_text);
        container.appendChild(button);
    }

    function create_subs_buttons(data) {
        const screenWidth = window.innerWidth;
        var container = document.getElementById('buttons-container');
        for (var i = 0; i < data.subs.length; i++) {
            (function(index) {
                let ext = data.subs[i].ext;
                let url = data.subs[i].url;
                let name = data.subs[i].name;
                let donate_flag = data.subs[i].donate_flag;
                let fname = data.media_desc;

                var button = document.createElement('button');
                button.type = 'button';
                
                button.className = 'btn btn-lg btn gray display-block col-3-5';
                if (screenWidth <= 768) {
                    button.className = 'btn btn-lg btn gray display-block col-11';
                }
                button.id = 'button-sub-' + i;
                button.style= 'margin-left: 10px;';
                let Id = button.id

                let btnText = "Subtitle";
                btnText += ` @`+ ext +` - ` + name;
                button.innerText = btnText;
                button.addEventListener('click', function() {
                    downloadVideoList(
                        Id,
                        url,
                        ext,
                        fname
                    );
                });
                if (donate_flag) {
                    button.className += " donate-icon-button tooltip-button";
                }
                if (url.length === 0) {
                    button.disabled = true;
                }
                container.appendChild(button);
            })(i);
        }
    }

   function show_author_div(data) {
       $("#user_card").removeClass("display-none");
       $("#video_author_head").css('background-image', 'url("' + data.author_head + '")');
       $("#video_author_nick").text(data.author_name);
       $("#video_author_art").text(data.media_desc);
   }
</script>
<script>
    
    var listItemHeight;
    function fetchBarrageData() {
        
        var apiEndpoint = "\/api\/batch_get_barrages";

        
        $.ajax({
            url: apiEndpoint,
            type: 'POST',
            async: true,
            contentType: 'application/json', 
            data: JSON.stringify({ batchSize: 10 }), 
            beforeSend: function () {
                
                $('#hot-media-list').addClass('loading');
            },
            success: function(response) {
                
                
                $('#hot-media-list').removeClass('loading');
                var $list = $("#hot-media-list").empty();
                var fragment = document.createDocumentFragment();
                
                var listItemHeight = 0;
                $.each(response.list, function(i, item) {
                    let tsBefore = timeDifference(item.update_time,response.now)

                    var $li;
                    if (item.appid === 2) {
                        $li = $("<li class='hot-yt-icon'></li>");
                    } else if (item.appid === 3){
                        $li = $("<li class='hot-bz-icon'></li>");
                    } else if (item.appid === 4) {
                        $li = $("<li class='hot-suno-icon'></li>");
                   

--- OCCURRENCE 4 (offset 172331) ---
ar button = document.createElement('button');
        button.type = 'button';
        
        button.className = 'btn btn-lg btn accent display-block col-3-5';
        if (screenWidth <= 768) {
            button.className = 'btn btn-lg btn accent display-block col-11';
        }
        button.id = 'button-cover';
        button.style= 'margin-left: 10px;';
        let Id = button.id
        let one_click_text = `downloadVideoList('${Id}','${url}','webp','cover')`

        button.innerText = "🏔️ Sampul video";
        button.setAttribute('onclick', one_click_text);
        container.appendChild(button);
    }

    function create_subs_buttons(data) {
        const screenWidth = window.innerWidth;
        var container = document.getElementById('buttons-container');
        for (var i = 0; i < data.subs.length; i++) {
            (function(index) {
                let ext = data.subs[i].ext;
                let url = data.subs[i].url;
                let name = data.subs[i].name;
                let donate_flag = data.subs[i].donate_flag;
                let fname = data.media_desc;

                var button = document.createElement('button');
                button.type = 'button';
                
                button.className = 'btn btn-lg btn gray display-block col-3-5';
                if (screenWidth <= 768) {
                    button.className = 'btn btn-lg btn gray display-block col-11';
                }
                button.id = 'button-sub-' + i;
                button.style= 'margin-left: 10px;';
                let Id = button.id

                let btnText = "Subtitle";
                btnText += ` @`+ ext +` - ` + name;
                button.innerText = btnText;
                button.addEventListener('click', function() {
                    downloadVideoList(
                        Id,
                        url,
                        ext,
                        fname
                    );
                });
                if (donate_flag) {
                    button.className += " donate-icon-button tooltip-button";
                }
                if (url.length === 0) {
                    button.disabled = true;
                }
                container.appendChild(button);
            })(i);
        }
    }

   function show_author_div(data) {
       $("#user_card").removeClass("display-none");
       $("#video_author_head").css('background-image', 'url("' + data.author_head + '")');
       $("#video_author_nick").text(data.author_name);
       $("#video_author_art").text(data.media_desc);
   }
</script>
<script>
    
    var listItemHeight;
    function fetchBarrageData() {
        
        var apiEndpoint = "\/api\/batch_get_barrages";

        
        $.ajax({
            url: apiEndpoint,
            type: 'POST',
            async: true,
            contentType: 'application/json', 
            data: JSON.stringify({ batchSize: 10 }), 
            beforeSend: function () {
                
                $('#hot-media-list').addClass('loading');
            },
            success: function(response) {
                
                
                $('#hot-media-list').removeClass('loading');
                var $list = $("#hot-media-list").empty();
                var fragment = document.createDocumentFragment();
                
                var listItemHeight = 0;
                $.each(response.list, function(i, item) {
                    let tsBefore = timeDifference(item.update_time,response.now)

                    var $li;
                    if (item.appid === 2) {
                        $li = $("<li class='hot-yt-icon'></li>");
                    } else if (item.appid === 3){
                        $li = $("<li class='hot-bz-icon'></li>");
                    } else if (item.appid === 4) {
                        $li = $("<li class='hot-suno-icon'></li>");
                    } else if (item.appid === 5) {
                        $li = $("<li class='hot-kuaishou-icon'></li>");
                    } else if (item.appid === 6) {
                        $li = $("<li class='hot-instagram-icon'></li>");
                    } else if (item.appid === 7) {
                        $li = $("<li class='hot-xhs-icon'></li>");
					} else if (item.appid === 8) {
                        $li = $("<li class='hot-facebook-icon'></li>");
                    } else if (item.appid === 9) {
                        $li = $("<li class='hot-twitter-icon'></li>");
                    } else if (item.appid === 10) {
                        $li = $("<li class='hot-pinterest-icon'></li>");
                    } else {
                        $li = $("<li></li>");
                    }

                    $li.append($("<div></div>", { "class": "user-avatar", "style": "background-image: url('" + item.author_head + "');" }));
                    $li.append($("<span></span>", { "class": "nickname", "text": item.author_name }));
                    var $divText = $("<div class='barrage-list'><div/>");
                    $divText.append($('<span class="visitor"><i class="icon icon-fire"></i>'+tsBefore+`, `+item.visitor.country+`-`+item.visitor.city + ` user downloaded:` + '</span>'));
                    
                    $divText.append($(`<a class="media-u

######################################################################
TARGET: current_download_url_for_play
OCCURRENCES: 2
######################################################################

--- OCCURRENCE 1 (offset 133277) ---
;

        function enable_post_btn() {
            $("#order_submit").removeAttr('disabled');
            $("#order_more_setting").removeAttr('disabled');
            order_is_running = 0;
        }

        function enable_post_container() {
            $("#download_player_div").removeAttr('hidden');
            $("#download_player_div").show();
            $('html,body').animate({scrollTop: $("#download_player_div").offset().top - 10}, 800)
        }

        function show_download_btn() {
            $("#download_btn").removeAttr('hidden');
            $("#download_btn").show();
            $("#buy_coffee").hide(); 
        }

        function show_video_download_btn(data) {
            $("#download_btn").hide();
            create_video_buttons(data);
            create_cover_button(data.video_cover);
            create_subs_buttons(data);
        }

        function bindDownloadURL(addr, fileType, title) {
            const downloadBtn = document.getElementById('download_btn');
            const newBtn = downloadBtn.cloneNode(true);
            downloadBtn.parentNode.replaceChild(newBtn, downloadBtn);
            newBtn.addEventListener('click', function() {
                downloadFileProV2(
                    addr,
                    fileType,
                    title
                );
            });
        }

        function play_and_pause_mp3() {
            var audioEle = document.getElementById("download_player_audio");
            audioEle.load();

            window.wavesurfer.on('ready', function () {
                console.log('音频加载成功，WaveSurfer 准备就绪');
                if (audioEle.paused) {
                    audioEle.play();
                } else {
                    audioEle.pause();
                }
            });
        }

        var current_download_url_for_play = '';
        var post_to_backend = function submitOxyerForm() {
            order_is_running = 1;
            var user_input_text_str = $("#UserInputTextarea").val();
            user_input_text_str = user_input_text_str.trim()
            if (!user_input_text_str) {
                showWarnMsgPrompt(messages1);
                $("#UserInputTextarea").css('border', '1px solid red');
                return 0
            }

            
            let user_select_app = $("input[name='RadioUserSelectAppID']:checked").attr('data-app-id');
            let user_select_app_id = $("input[name='RadioUserSelectAppID']:checked").val();
            user_select_app = "bilibili";
            user_select_app_id = 3;
            if (!user_select_app_id) {
                toastr.warning('Please choose the app you like')
                return 0
            }

            var user_select_setting_audio_format = $("input[name='RadioUserSelectMediaFormat']:checked").val();
            let user_select_announcer_input_text_limit_count = $("input[name='RadioUserSelectAppID']:checked").attr('data-limit-text-count');

            if (user_input_text_str.length > user_select_announcer_input_text_limit_count) {
                toastr.warning('The text you entered is too long, The maximum text limit is exceeded, Change a voice, or reduce the length of the text')

                $("#UserInputTextarea").css('border-color', 'red');
                $("#UserInputTextarea").css('background-color', '#ffe6e6');
                order_is_running = 0;
                return 0
            } else {
                $("#UserInputTextarea").css('border-color', '');
                $("#UserInputTextarea").css('background-color', '');
            }

            let user_anonymous = $("#userSelectAnonymousMode").checked;
            let user_input_captcha_text = $("#UserInputCaptcha").val();

            if (!user_input_captcha_text) {
                showWarnMsgPrompt(messages2);

                $("#UserInputCaptcha").focus();
                $("#UserInputCaptcha").css('border', '1px solid red');
                $("#UserInputCaptcha").css('background-color', '#ffcccc');
                order_is_running = 0;
                return 0
            } else {
                $("#UserInputCaptcha").css('border', '1px solid #ced4da');
                $("#UserInputCaptcha").css('background-color', '#fff');
            }


            let user_uuid_text = $("#VerifyCaptchaIMG").attr('uuid');
            if (!user_uuid_text) {
                toastr.warning('Unable to obtain UUID, please try again or change browser')
                return 0
            }

            let user_select_announcer_is_need_queue = $("input[name='RadioUserSelectAppID']:checked").attr('data-is-need-queue');
            console.log(user_select_announcer_is_need_queue);

            if (user_select_announcer_is_need_queue == 'true') {
                console.log('need queue')



            } else {
                console.log('not need queue')

            }

            let targetUrl = '\/api\/create_oxy_order'
            let dataJSON = {}
            let uuid = getCookieValue("uuid");
            if (uuid != "") {
                user_uuid_text = uuid;
                console.log("uuid:"+user_uuid_text);
            }
            dataJSON['user_uuid_text'] = user_uuid_text
            dataJSON['user_input_text'] = user_input_text_str
            
            
            dataJSON['u

--- OCCURRENCE 2 (offset 167515) ---
str.warning, 3000, "It has been detected that image resources are included and has been switched to the album for you.");
               show_images_div(datatemp);
               return;
           }
           show_video_download_btn(data);
           check_hide_afd_button();
           injectAd(data);
           $("#download_player_anim_panel").hide();
           $("#dl_wrapper").removeClass('display-none');
           initFluidPlayer(data.video_base_url, data.video_cover);
           try {
               var task_videoUrl = data.video_base_url;
               var task_text = data.media_desc;
               var task_validityPeriod = 30;
               var task_appName = data.post_data.user_select_app;
               var task_mediaFormat = data.media_format;

               THPaddHistory(task_videoUrl, task_text, task_validityPeriod, task_appName, task_mediaFormat);
           } catch (e) {
               console.log("THPaddHistory exception:"+e)
           }
       }
   }

   function show_audio_div(data) {
       if (data.media_format === "mp3") {

           if (!data.auto_mute_share && data.auto_stand_url === "") {
               $("#download_btn").prop('disabled', true);
               setTimeout(toastr.warning, 3000, data.auto_offline_desc);
           }

           show_download_btn();
           injectAd(data);
           try {
               var this_task_return_mp3_url = data.auto_stand_url
               window.wavesurfer.load(data.auto_stand_url)
               window.wavesurfer.saveurl = data.auto_stand_url
           } catch (e) {
               console.log(e)
           }

           $("#download_player_anim_panel").hide();
           $("#waveform").removeClass("display-none")
           $("#download_player_audio").removeClass("display-none")
           current_download_url_for_play = data.auto_stand_url
           $("#download_player_audio").attr('src', data.auto_stand_url);
           $("#download_player_source").attr('src', data.auto_stand_url);
           $("#download_player_source").attr('type', "audio/" + data.audio_format);
           play_and_pause_mp3();
           bindDownloadURL(data.auto_stand_url, data.media_format, data.media_desc);
           create_afd_button();
           try {
               var task_audioUrl = data.auto_stand_url;
               var task_text = data.media_desc;
               var task_validityPeriod = 30;
               var task_appName = data.post_data.user_select_app;
               var task_mediaFormat = data.media_format;

               if (task_audioUrl.length > 0) {
                   THPaddHistory(task_audioUrl, task_text, task_validityPeriod, task_appName, task_mediaFormat);
               }
           } catch (e) {
               console.log("THPaddHistory exception:"+e)
           }
       }
   }

   function show_images_div(data) {
       if (data.media_format === "webp" || data.media_format === "jpeg") {
           if (data.media_type != 2 || data.image_url_list.length == 0) {
               let datatemp = JSON.parse(JSON.stringify(data));
               datatemp.media_format = "mp4"
               setTimeout(toastr.warning, 3000, "检测到包含视频资源，已为您切换至视频");
               show_video_div(datatemp);
               return;
           }

           show_download_btn();
           console.info("data.image_url_list:"+data.image_url_list);
           $("#download_player_anim_panel").hide();
           $("#gallery").show();
           const gallery = document.getElementById('gallery');
           gallery.innerHTML = '';
           data.image_url_list.forEach(image => {
               
               const itemHTML = `
      <div class="grid-item">
        <a href="${image}" data-lightbox="gallery" data-title="${image}">
          <img src="${image}" alt="${image}" style="width: 100%; height: auto;">
        </a>
      </div>
    `;
               gallery.insertAdjacentHTML('beforeend', itemHTML);
           });

           
           bindDownloadURL('', data.media_format, data.media_desc);
           create_afd_button();
           try {
               var task_imagesUrl = data.image_url_list;
               var task_text = data.media_desc;
               var task_validityPeriod = 30;
               var task_appName = data.post_data.user_select_app;
               var task_mediaFormat = data.media_format;

               THPaddHistory(task_imagesUrl, task_text, task_validityPeriod, task_appName, task_mediaFormat);
           } catch (e) {
               console.log("THPaddHistory exception:"+e)
           }
       }
   }

    function create_cover_button(cover) {
        const url = cover
        if (url.length === 0) {
            return;
        }
        const screenWidth = window.innerWidth;
        var container = document.getElementById('buttons-container');
        var button = document.createElement('button');
        button.type = 'button';
        
        button.className = 'btn btn-lg btn accent display-block col-3-5';
        if (screenWidth <= 768) {
            button.className = 'btn btn-lg btn accent display-block col-11';
        }
        button.id = 'button-cover';
        button.style= 'margin-left: 10px;';
        let Id = button.id
        let one_click_text = `downloadVideoList('${Id}','${url}','webp','cover')`

        butt

######################################################################
TARGET: history_download_media_url
OCCURRENCES: 2
######################################################################

--- OCCURRENCE 1 (offset 148469) ---
 publik)';
                if ($(this).is(':checked')) {

                    new_oxy_order_submit_text = oxy_order_submit_text + try_anonymous_mode_text;

                    $('#order_submit').html(new_oxy_order_submit_text);
                } else {
                    new_oxy_order_submit_text = oxy_order_submit_text.replace(try_anonymous_mode_text, '');

                    $('#order_submit').html(new_oxy_order_submit_text);
                }
            });

        });

    </script>
<script>
        function clearTextareaWithFastTypewriterAnimation() {
            var textarea = $("#UserInputTextarea");
            var text = textarea.val();
            var length = text.length;
            var startTime = performance.now();
            var duration = 2000;
            
            if (length < 1000) {
                duration = 1000;
            }

            function deleteText(timestamp) {
                var progress = timestamp - startTime;

                if (progress < duration) {
                    var index = Math.floor((length * progress) / duration);
                    textarea.val(text.substring(0, length - index).trim());
                    requestAnimationFrame(deleteText);
                } else {
                    
                    textarea.removeClass("typewriter-animation");
                    textarea.val("");
                    localStorage.removeItem('bilibili_autosave_text');

                }
                
                inputStatistics();
            }
            
            textarea.focus();
            textarea.addClass("typewriter-animation");
            requestAnimationFrame(deleteText);
            
            $('#clear_textarea_btn').hide();


        }
    </script>
<script>

            function THPaddHistory(history_download_media_url, original_text, localstorage_validity_period, appName, mediaFormat) {
                console.info("call THPaddHistory");
                const currentTime = new Date().getTime();
                const expirationTime = currentTime + localstorage_validity_period * 60 * 1000;
                const historyItem = {
                    mediaUrl: history_download_media_url,
                    text: original_text,
                    expiration: expirationTime,
                    appName: appName,
                    mediaFormat: mediaFormat,
                };
                
                let historyList = localStorage.getItem('downloadHistory');
                if (historyList) {
                    historyList = JSON.parse(historyList);
                } else {
                    historyList = [];
                }

                
                const existingItemIndex = historyList.findIndex(item => (item.mediaUrl == historyItem.mediaUrl) || (item.text == historyItem.text));
                if (existingItemIndex === -1) {
                    
                    historyList.push(historyItem);
                } else {
                    
                    historyList[existingItemIndex] = historyItem; 
                }
                localStorage.setItem('downloadHistory', JSON.stringify(historyList));
                let user_history_download_count_span = THPgetAllHistory().length;
                $('.user_history_download_count_span').text(user_history_download_count_span);
            }

            function THPgetAllHistory() {
                
                let historyList = localStorage.getItem('downloadHistory');
                if (historyList) {
                    historyList = JSON.parse(historyList);
                    const currentTime = new Date().getTime();
                    historyList = historyList.filter(historyItem => historyItem.expiration > currentTime);
                    localStorage.setItem('downloadHistory', JSON.stringify(historyList));
                    historyList.reverse();
                    return historyList;
                } else {
                    return [];
                }
            }
            

            function THPupdatePanelDOM() {
                
                let allHistory = THPgetAllHistory();
                const historyPanelList = document.getElementById('download_history_panel_list');
                historyPanelList.innerHTML = '';
                if (allHistory.length === 0) {
                    historyPanelList.innerHTML = '';
                    historyPanelList.innerHTML = '<p class="mx-2 py-3">No valid history records found in the last 30 minutes.</p>';
                    return;
                }

                
                allHistory.forEach((historyItem, index) => {
                    
                    let listItem = document.createElement('div');
                    listItem.classList.add('list-group-item');
                    listItem.classList.add('ai-recommended-bg-04');
                    let audioType = historyItem.audioUrl.split('.').pop();
                    let task_time_str = new Date(historyItem.expiration).toLocaleString();

                    function formatDate(timestamp) {
                        if (!timestamp) {
                            return '1970-01-01 00:00';
                        }
                        
                        let date = new Date(parseInt(timestamp));
       

--- OCCURRENCE 2 (offset 148839) ---
            $('#order_submit').html(new_oxy_order_submit_text);
                }
            });

        });

    </script>
<script>
        function clearTextareaWithFastTypewriterAnimation() {
            var textarea = $("#UserInputTextarea");
            var text = textarea.val();
            var length = text.length;
            var startTime = performance.now();
            var duration = 2000;
            
            if (length < 1000) {
                duration = 1000;
            }

            function deleteText(timestamp) {
                var progress = timestamp - startTime;

                if (progress < duration) {
                    var index = Math.floor((length * progress) / duration);
                    textarea.val(text.substring(0, length - index).trim());
                    requestAnimationFrame(deleteText);
                } else {
                    
                    textarea.removeClass("typewriter-animation");
                    textarea.val("");
                    localStorage.removeItem('bilibili_autosave_text');

                }
                
                inputStatistics();
            }
            
            textarea.focus();
            textarea.addClass("typewriter-animation");
            requestAnimationFrame(deleteText);
            
            $('#clear_textarea_btn').hide();


        }
    </script>
<script>

            function THPaddHistory(history_download_media_url, original_text, localstorage_validity_period, appName, mediaFormat) {
                console.info("call THPaddHistory");
                const currentTime = new Date().getTime();
                const expirationTime = currentTime + localstorage_validity_period * 60 * 1000;
                const historyItem = {
                    mediaUrl: history_download_media_url,
                    text: original_text,
                    expiration: expirationTime,
                    appName: appName,
                    mediaFormat: mediaFormat,
                };
                
                let historyList = localStorage.getItem('downloadHistory');
                if (historyList) {
                    historyList = JSON.parse(historyList);
                } else {
                    historyList = [];
                }

                
                const existingItemIndex = historyList.findIndex(item => (item.mediaUrl == historyItem.mediaUrl) || (item.text == historyItem.text));
                if (existingItemIndex === -1) {
                    
                    historyList.push(historyItem);
                } else {
                    
                    historyList[existingItemIndex] = historyItem; 
                }
                localStorage.setItem('downloadHistory', JSON.stringify(historyList));
                let user_history_download_count_span = THPgetAllHistory().length;
                $('.user_history_download_count_span').text(user_history_download_count_span);
            }

            function THPgetAllHistory() {
                
                let historyList = localStorage.getItem('downloadHistory');
                if (historyList) {
                    historyList = JSON.parse(historyList);
                    const currentTime = new Date().getTime();
                    historyList = historyList.filter(historyItem => historyItem.expiration > currentTime);
                    localStorage.setItem('downloadHistory', JSON.stringify(historyList));
                    historyList.reverse();
                    return historyList;
                } else {
                    return [];
                }
            }
            

            function THPupdatePanelDOM() {
                
                let allHistory = THPgetAllHistory();
                const historyPanelList = document.getElementById('download_history_panel_list');
                historyPanelList.innerHTML = '';
                if (allHistory.length === 0) {
                    historyPanelList.innerHTML = '';
                    historyPanelList.innerHTML = '<p class="mx-2 py-3">No valid history records found in the last 30 minutes.</p>';
                    return;
                }

                
                allHistory.forEach((historyItem, index) => {
                    
                    let listItem = document.createElement('div');
                    listItem.classList.add('list-group-item');
                    listItem.classList.add('ai-recommended-bg-04');
                    let audioType = historyItem.audioUrl.split('.').pop();
                    let task_time_str = new Date(historyItem.expiration).toLocaleString();

                    function formatDate(timestamp) {
                        if (!timestamp) {
                            return '1970-01-01 00:00';
                        }
                        
                        let date = new Date(parseInt(timestamp));
                        date = new Date(date.getTime() - 30 * 60 * 1000);
                        let year = date.getFullYear();
                        let month = ('0' + (date.getMonth() + 1)).slice(-2);
                        let day = ('0' + date.getDate()).slice(-2);
                        let hours = ('0' + date.getHours()).slice(-2);
                        let minu

######################################################################
TARGET: task_videoUrl
OCCURRENCES: 2
######################################################################

--- OCCURRENCE 1 (offset 166232) ---
tion() {
            console.error('An error occurred during the request. (timeout)');
            window.open(url, '_blank');
        };

        x.send();

        setTimeout(function () {
            el.removeAttr('disabled');
            el.text(download_btn_old_text);
            el.css('display', 'inline');
            el.prop('disabled', false);
            el.attr('onclick', download_btn_old_onclick);
        }, 30000);

        try {

            let full_event_text = 'Download File'
            console.log(full_event_text)
            mixpanel.track(full_event_text, {
                'event': 'Download File',
                'url': url,
            })
        } catch (e) {
            console.log(e)
        }
    }

    function show_all_media_div(data) {
        var container = document.getElementById('buttons-container');
        container.innerHTML = '';
        resetAd();
        
        show_video_div(data);

        
        show_images_div(data);

        
        show_audio_div(data);
    }

    function show_video_div(data) {
       if (data.media_format === "mp4") {
           if (data.media_type === 2) {
               let datatemp = JSON.parse(JSON.stringify(data));
               datatemp.media_format = "jpeg"
               setTimeout(toastr.warning, 3000, "It has been detected that image resources are included and has been switched to the album for you.");
               show_images_div(datatemp);
               return;
           }
           show_video_download_btn(data);
           check_hide_afd_button();
           injectAd(data);
           $("#download_player_anim_panel").hide();
           $("#dl_wrapper").removeClass('display-none');
           initFluidPlayer(data.video_base_url, data.video_cover);
           try {
               var task_videoUrl = data.video_base_url;
               var task_text = data.media_desc;
               var task_validityPeriod = 30;
               var task_appName = data.post_data.user_select_app;
               var task_mediaFormat = data.media_format;

               THPaddHistory(task_videoUrl, task_text, task_validityPeriod, task_appName, task_mediaFormat);
           } catch (e) {
               console.log("THPaddHistory exception:"+e)
           }
       }
   }

   function show_audio_div(data) {
       if (data.media_format === "mp3") {

           if (!data.auto_mute_share && data.auto_stand_url === "") {
               $("#download_btn").prop('disabled', true);
               setTimeout(toastr.warning, 3000, data.auto_offline_desc);
           }

           show_download_btn();
           injectAd(data);
           try {
               var this_task_return_mp3_url = data.auto_stand_url
               window.wavesurfer.load(data.auto_stand_url)
               window.wavesurfer.saveurl = data.auto_stand_url
           } catch (e) {
               console.log(e)
           }

           $("#download_player_anim_panel").hide();
           $("#waveform").removeClass("display-none")
           $("#download_player_audio").removeClass("display-none")
           current_download_url_for_play = data.auto_stand_url
           $("#download_player_audio").attr('src', data.auto_stand_url);
           $("#download_player_source").attr('src', data.auto_stand_url);
           $("#download_player_source").attr('type', "audio/" + data.audio_format);
           play_and_pause_mp3();
           bindDownloadURL(data.auto_stand_url, data.media_format, data.media_desc);
           create_afd_button();
           try {
               var task_audioUrl = data.auto_stand_url;
               var task_text = data.media_desc;
               var task_validityPeriod = 30;
               var task_appName = data.post_data.user_select_app;
               var task_mediaFormat = data.media_format;

               if (task_audioUrl.length > 0) {
                   THPaddHistory(task_audioUrl, task_text, task_validityPeriod, task_appName, task_mediaFormat);
               }
           } catch (e) {
               console.log("THPaddHistory exception:"+e)
           }
       }
   }

   function show_images_div(data) {
       if (data.media_format === "webp" || data.media_format === "jpeg") {
           if (data.media_type != 2 || data.image_url_list.length == 0) {
               let datatemp = JSON.parse(JSON.stringify(data));
               datatemp.media_format = "mp4"
               setTimeout(toastr.warning, 3000, "检测到包含视频资源，已为您切换至视频");
               show_video_div(datatemp);
               return;
           }

           show_download_btn();
           console.info("data.image_url_list:"+data.image_url_list);
           $("#download_player_anim_panel").hide();
           $("#gallery").show();
           const gallery = document.getElementById('gallery');
           gallery.innerHTML = '';
           data.image_url_list.forEach(image => {
               
               const itemHTML = `
      <div class="grid-item">
        <a href="${image}" data-lightbox="gallery" data-title="${image}">
          <img src="${image}" alt="${image}" style="width: 100%; height: auto;">
        </a>
      </div>
    `;
               gallery.insertAdjacentHTML('beforeend', itemHTML);
           });

           
           bindDownloadURL('', data.media_format, data.media_desc);


--- OCCURRENCE 2 (offset 166515) ---
   el.css('display', 'inline');
            el.prop('disabled', false);
            el.attr('onclick', download_btn_old_onclick);
        }, 30000);

        try {

            let full_event_text = 'Download File'
            console.log(full_event_text)
            mixpanel.track(full_event_text, {
                'event': 'Download File',
                'url': url,
            })
        } catch (e) {
            console.log(e)
        }
    }

    function show_all_media_div(data) {
        var container = document.getElementById('buttons-container');
        container.innerHTML = '';
        resetAd();
        
        show_video_div(data);

        
        show_images_div(data);

        
        show_audio_div(data);
    }

    function show_video_div(data) {
       if (data.media_format === "mp4") {
           if (data.media_type === 2) {
               let datatemp = JSON.parse(JSON.stringify(data));
               datatemp.media_format = "jpeg"
               setTimeout(toastr.warning, 3000, "It has been detected that image resources are included and has been switched to the album for you.");
               show_images_div(datatemp);
               return;
           }
           show_video_download_btn(data);
           check_hide_afd_button();
           injectAd(data);
           $("#download_player_anim_panel").hide();
           $("#dl_wrapper").removeClass('display-none');
           initFluidPlayer(data.video_base_url, data.video_cover);
           try {
               var task_videoUrl = data.video_base_url;
               var task_text = data.media_desc;
               var task_validityPeriod = 30;
               var task_appName = data.post_data.user_select_app;
               var task_mediaFormat = data.media_format;

               THPaddHistory(task_videoUrl, task_text, task_validityPeriod, task_appName, task_mediaFormat);
           } catch (e) {
               console.log("THPaddHistory exception:"+e)
           }
       }
   }

   function show_audio_div(data) {
       if (data.media_format === "mp3") {

           if (!data.auto_mute_share && data.auto_stand_url === "") {
               $("#download_btn").prop('disabled', true);
               setTimeout(toastr.warning, 3000, data.auto_offline_desc);
           }

           show_download_btn();
           injectAd(data);
           try {
               var this_task_return_mp3_url = data.auto_stand_url
               window.wavesurfer.load(data.auto_stand_url)
               window.wavesurfer.saveurl = data.auto_stand_url
           } catch (e) {
               console.log(e)
           }

           $("#download_player_anim_panel").hide();
           $("#waveform").removeClass("display-none")
           $("#download_player_audio").removeClass("display-none")
           current_download_url_for_play = data.auto_stand_url
           $("#download_player_audio").attr('src', data.auto_stand_url);
           $("#download_player_source").attr('src', data.auto_stand_url);
           $("#download_player_source").attr('type', "audio/" + data.audio_format);
           play_and_pause_mp3();
           bindDownloadURL(data.auto_stand_url, data.media_format, data.media_desc);
           create_afd_button();
           try {
               var task_audioUrl = data.auto_stand_url;
               var task_text = data.media_desc;
               var task_validityPeriod = 30;
               var task_appName = data.post_data.user_select_app;
               var task_mediaFormat = data.media_format;

               if (task_audioUrl.length > 0) {
                   THPaddHistory(task_audioUrl, task_text, task_validityPeriod, task_appName, task_mediaFormat);
               }
           } catch (e) {
               console.log("THPaddHistory exception:"+e)
           }
       }
   }

   function show_images_div(data) {
       if (data.media_format === "webp" || data.media_format === "jpeg") {
           if (data.media_type != 2 || data.image_url_list.length == 0) {
               let datatemp = JSON.parse(JSON.stringify(data));
               datatemp.media_format = "mp4"
               setTimeout(toastr.warning, 3000, "检测到包含视频资源，已为您切换至视频");
               show_video_div(datatemp);
               return;
           }

           show_download_btn();
           console.info("data.image_url_list:"+data.image_url_list);
           $("#download_player_anim_panel").hide();
           $("#gallery").show();
           const gallery = document.getElementById('gallery');
           gallery.innerHTML = '';
           data.image_url_list.forEach(image => {
               
               const itemHTML = `
      <div class="grid-item">
        <a href="${image}" data-lightbox="gallery" data-title="${image}">
          <img src="${image}" alt="${image}" style="width: 100%; height: auto;">
        </a>
      </div>
    `;
               gallery.insertAdjacentHTML('beforeend', itemHTML);
           });

           
           bindDownloadURL('', data.media_format, data.media_desc);
           create_afd_button();
           try {
               var task_imagesUrl = data.image_url_list;
               var task_text = data.media_desc;
               var task_validityPeriod = 30;
               var task_appName = data.post_data.user_select_app;
               var

######################################################################
TARGET: task_mediaFormat
OCCURRENCES: 6
######################################################################

--- OCCURRENCE 1 (offset 166447) ---
r('disabled');
            el.text(download_btn_old_text);
            el.css('display', 'inline');
            el.prop('disabled', false);
            el.attr('onclick', download_btn_old_onclick);
        }, 30000);

        try {

            let full_event_text = 'Download File'
            console.log(full_event_text)
            mixpanel.track(full_event_text, {
                'event': 'Download File',
                'url': url,
            })
        } catch (e) {
            console.log(e)
        }
    }

    function show_all_media_div(data) {
        var container = document.getElementById('buttons-container');
        container.innerHTML = '';
        resetAd();
        
        show_video_div(data);

        
        show_images_div(data);

        
        show_audio_div(data);
    }

    function show_video_div(data) {
       if (data.media_format === "mp4") {
           if (data.media_type === 2) {
               let datatemp = JSON.parse(JSON.stringify(data));
               datatemp.media_format = "jpeg"
               setTimeout(toastr.warning, 3000, "It has been detected that image resources are included and has been switched to the album for you.");
               show_images_div(datatemp);
               return;
           }
           show_video_download_btn(data);
           check_hide_afd_button();
           injectAd(data);
           $("#download_player_anim_panel").hide();
           $("#dl_wrapper").removeClass('display-none');
           initFluidPlayer(data.video_base_url, data.video_cover);
           try {
               var task_videoUrl = data.video_base_url;
               var task_text = data.media_desc;
               var task_validityPeriod = 30;
               var task_appName = data.post_data.user_select_app;
               var task_mediaFormat = data.media_format;

               THPaddHistory(task_videoUrl, task_text, task_validityPeriod, task_appName, task_mediaFormat);
           } catch (e) {
               console.log("THPaddHistory exception:"+e)
           }
       }
   }

   function show_audio_div(data) {
       if (data.media_format === "mp3") {

           if (!data.auto_mute_share && data.auto_stand_url === "") {
               $("#download_btn").prop('disabled', true);
               setTimeout(toastr.warning, 3000, data.auto_offline_desc);
           }

           show_download_btn();
           injectAd(data);
           try {
               var this_task_return_mp3_url = data.auto_stand_url
               window.wavesurfer.load(data.auto_stand_url)
               window.wavesurfer.saveurl = data.auto_stand_url
           } catch (e) {
               console.log(e)
           }

           $("#download_player_anim_panel").hide();
           $("#waveform").removeClass("display-none")
           $("#download_player_audio").removeClass("display-none")
           current_download_url_for_play = data.auto_stand_url
           $("#download_player_audio").attr('src', data.auto_stand_url);
           $("#download_player_source").attr('src', data.auto_stand_url);
           $("#download_player_source").attr('type', "audio/" + data.audio_format);
           play_and_pause_mp3();
           bindDownloadURL(data.auto_stand_url, data.media_format, data.media_desc);
           create_afd_button();
           try {
               var task_audioUrl = data.auto_stand_url;
               var task_text = data.media_desc;
               var task_validityPeriod = 30;
               var task_appName = data.post_data.user_select_app;
               var task_mediaFormat = data.media_format;

               if (task_audioUrl.length > 0) {
                   THPaddHistory(task_audioUrl, task_text, task_validityPeriod, task_appName, task_mediaFormat);
               }
           } catch (e) {
               console.log("THPaddHistory exception:"+e)
           }
       }
   }

   function show_images_div(data) {
       if (data.media_format === "webp" || data.media_format === "jpeg") {
           if (data.media_type != 2 || data.image_url_list.length == 0) {
               let datatemp = JSON.parse(JSON.stringify(data));
               datatemp.media_format = "mp4"
               setTimeout(toastr.warning, 3000, "检测到包含视频资源，已为您切换至视频");
               show_video_div(datatemp);
               return;
           }

           show_download_btn();
           console.info("data.image_url_list:"+data.image_url_list);
           $("#download_player_anim_panel").hide();
           $("#gallery").show();
           const gallery = document.getElementById('gallery');
           gallery.innerHTML = '';
           data.image_url_list.forEach(image => {
               
               const itemHTML = `
      <div class="grid-item">
        <a href="${image}" data-lightbox="gallery" data-title="${image}">
          <img src="${image}" alt="${image}" style="width: 100%; height: auto;">
        </a>
      </div>
    `;
               gallery.insertAdjacentHTML('beforeend', itemHTML);
           });

           
           bindDownloadURL('', data.media_format, data.media_desc);
           create_afd_button();
           try {
               var task_imagesUrl = data.image_url_list;
               var task_text = data.media_desc;
               var task_validityPeriod = 30;
               v

--- OCCURRENCE 2 (offset 166576) ---
', false);
            el.attr('onclick', download_btn_old_onclick);
        }, 30000);

        try {

            let full_event_text = 'Download File'
            console.log(full_event_text)
            mixpanel.track(full_event_text, {
                'event': 'Download File',
                'url': url,
            })
        } catch (e) {
            console.log(e)
        }
    }

    function show_all_media_div(data) {
        var container = document.getElementById('buttons-container');
        container.innerHTML = '';
        resetAd();
        
        show_video_div(data);

        
        show_images_div(data);

        
        show_audio_div(data);
    }

    function show_video_div(data) {
       if (data.media_format === "mp4") {
           if (data.media_type === 2) {
               let datatemp = JSON.parse(JSON.stringify(data));
               datatemp.media_format = "jpeg"
               setTimeout(toastr.warning, 3000, "It has been detected that image resources are included and has been switched to the album for you.");
               show_images_div(datatemp);
               return;
           }
           show_video_download_btn(data);
           check_hide_afd_button();
           injectAd(data);
           $("#download_player_anim_panel").hide();
           $("#dl_wrapper").removeClass('display-none');
           initFluidPlayer(data.video_base_url, data.video_cover);
           try {
               var task_videoUrl = data.video_base_url;
               var task_text = data.media_desc;
               var task_validityPeriod = 30;
               var task_appName = data.post_data.user_select_app;
               var task_mediaFormat = data.media_format;

               THPaddHistory(task_videoUrl, task_text, task_validityPeriod, task_appName, task_mediaFormat);
           } catch (e) {
               console.log("THPaddHistory exception:"+e)
           }
       }
   }

   function show_audio_div(data) {
       if (data.media_format === "mp3") {

           if (!data.auto_mute_share && data.auto_stand_url === "") {
               $("#download_btn").prop('disabled', true);
               setTimeout(toastr.warning, 3000, data.auto_offline_desc);
           }

           show_download_btn();
           injectAd(data);
           try {
               var this_task_return_mp3_url = data.auto_stand_url
               window.wavesurfer.load(data.auto_stand_url)
               window.wavesurfer.saveurl = data.auto_stand_url
           } catch (e) {
               console.log(e)
           }

           $("#download_player_anim_panel").hide();
           $("#waveform").removeClass("display-none")
           $("#download_player_audio").removeClass("display-none")
           current_download_url_for_play = data.auto_stand_url
           $("#download_player_audio").attr('src', data.auto_stand_url);
           $("#download_player_source").attr('src', data.auto_stand_url);
           $("#download_player_source").attr('type', "audio/" + data.audio_format);
           play_and_pause_mp3();
           bindDownloadURL(data.auto_stand_url, data.media_format, data.media_desc);
           create_afd_button();
           try {
               var task_audioUrl = data.auto_stand_url;
               var task_text = data.media_desc;
               var task_validityPeriod = 30;
               var task_appName = data.post_data.user_select_app;
               var task_mediaFormat = data.media_format;

               if (task_audioUrl.length > 0) {
                   THPaddHistory(task_audioUrl, task_text, task_validityPeriod, task_appName, task_mediaFormat);
               }
           } catch (e) {
               console.log("THPaddHistory exception:"+e)
           }
       }
   }

   function show_images_div(data) {
       if (data.media_format === "webp" || data.media_format === "jpeg") {
           if (data.media_type != 2 || data.image_url_list.length == 0) {
               let datatemp = JSON.parse(JSON.stringify(data));
               datatemp.media_format = "mp4"
               setTimeout(toastr.warning, 3000, "检测到包含视频资源，已为您切换至视频");
               show_video_div(datatemp);
               return;
           }

           show_download_btn();
           console.info("data.image_url_list:"+data.image_url_list);
           $("#download_player_anim_panel").hide();
           $("#gallery").show();
           const gallery = document.getElementById('gallery');
           gallery.innerHTML = '';
           data.image_url_list.forEach(image => {
               
               const itemHTML = `
      <div class="grid-item">
        <a href="${image}" data-lightbox="gallery" data-title="${image}">
          <img src="${image}" alt="${image}" style="width: 100%; height: auto;">
        </a>
      </div>
    `;
               gallery.insertAdjacentHTML('beforeend', itemHTML);
           });

           
           bindDownloadURL('', data.media_format, data.media_desc);
           create_afd_button();
           try {
               var task_imagesUrl = data.image_url_list;
               var task_text = data.media_desc;
               var task_validityPeriod = 30;
               var task_appName = data.post_data.user_select_app;
               var task_mediaFormat = data.media_format;

               THPadd

--- OCCURRENCE 3 (offset 168199) ---
a.post_data.user_select_app;
               var task_mediaFormat = data.media_format;

               THPaddHistory(task_videoUrl, task_text, task_validityPeriod, task_appName, task_mediaFormat);
           } catch (e) {
               console.log("THPaddHistory exception:"+e)
           }
       }
   }

   function show_audio_div(data) {
       if (data.media_format === "mp3") {

           if (!data.auto_mute_share && data.auto_stand_url === "") {
               $("#download_btn").prop('disabled', true);
               setTimeout(toastr.warning, 3000, data.auto_offline_desc);
           }

           show_download_btn();
           injectAd(data);
           try {
               var this_task_return_mp3_url = data.auto_stand_url
               window.wavesurfer.load(data.auto_stand_url)
               window.wavesurfer.saveurl = data.auto_stand_url
           } catch (e) {
               console.log(e)
           }

           $("#download_player_anim_panel").hide();
           $("#waveform").removeClass("display-none")
           $("#download_player_audio").removeClass("display-none")
           current_download_url_for_play = data.auto_stand_url
           $("#download_player_audio").attr('src', data.auto_stand_url);
           $("#download_player_source").attr('src', data.auto_stand_url);
           $("#download_player_source").attr('type', "audio/" + data.audio_format);
           play_and_pause_mp3();
           bindDownloadURL(data.auto_stand_url, data.media_format, data.media_desc);
           create_afd_button();
           try {
               var task_audioUrl = data.auto_stand_url;
               var task_text = data.media_desc;
               var task_validityPeriod = 30;
               var task_appName = data.post_data.user_select_app;
               var task_mediaFormat = data.media_format;

               if (task_audioUrl.length > 0) {
                   THPaddHistory(task_audioUrl, task_text, task_validityPeriod, task_appName, task_mediaFormat);
               }
           } catch (e) {
               console.log("THPaddHistory exception:"+e)
           }
       }
   }

   function show_images_div(data) {
       if (data.media_format === "webp" || data.media_format === "jpeg") {
           if (data.media_type != 2 || data.image_url_list.length == 0) {
               let datatemp = JSON.parse(JSON.stringify(data));
               datatemp.media_format = "mp4"
               setTimeout(toastr.warning, 3000, "检测到包含视频资源，已为您切换至视频");
               show_video_div(datatemp);
               return;
           }

           show_download_btn();
           console.info("data.image_url_list:"+data.image_url_list);
           $("#download_player_anim_panel").hide();
           $("#gallery").show();
           const gallery = document.getElementById('gallery');
           gallery.innerHTML = '';
           data.image_url_list.forEach(image => {
               
               const itemHTML = `
      <div class="grid-item">
        <a href="${image}" data-lightbox="gallery" data-title="${image}">
          <img src="${image}" alt="${image}" style="width: 100%; height: auto;">
        </a>
      </div>
    `;
               gallery.insertAdjacentHTML('beforeend', itemHTML);
           });

           
           bindDownloadURL('', data.media_format, data.media_desc);
           create_afd_button();
           try {
               var task_imagesUrl = data.image_url_list;
               var task_text = data.media_desc;
               var task_validityPeriod = 30;
               var task_appName = data.post_data.user_select_app;
               var task_mediaFormat = data.media_format;

               THPaddHistory(task_imagesUrl, task_text, task_validityPeriod, task_appName, task_mediaFormat);
           } catch (e) {
               console.log("THPaddHistory exception:"+e)
           }
       }
   }

    function create_cover_button(cover) {
        const url = cover
        if (url.length === 0) {
            return;
        }
        const screenWidth = window.innerWidth;
        var container = document.getElementById('buttons-container');
        var button = document.createElement('button');
        button.type = 'button';
        
        button.className = 'btn btn-lg btn accent display-block col-3-5';
        if (screenWidth <= 768) {
            button.className = 'btn btn-lg btn accent display-block col-11';
        }
        button.id = 'button-cover';
        button.style= 'margin-left: 10px;';
        let Id = button.id
        let one_click_text = `downloadVideoList('${Id}','${url}','webp','cover')`

        button.innerText = "🏔️ Sampul video";
        button.setAttribute('onclick', one_click_text);
        container.appendChild(button);
    }

    function create_subs_buttons(data) {
        const screenWidth = window.innerWidth;
        var container = document.getElementById('buttons-container');
        for (var i = 0; i < data.subs.length; i++) {
            (function(index) {
                let ext = data.subs[i].ext;
                let url = data.subs[i].url;
                let name = data.subs[i].name;
                let donate_flag = data.subs[i].donate_flag;
                let fname = data.media_desc;

                var button = document.createElement('button');
   

--- OCCURRENCE 4 (offset 168379) ---
k_mediaFormat);
           } catch (e) {
               console.log("THPaddHistory exception:"+e)
           }
       }
   }

   function show_audio_div(data) {
       if (data.media_format === "mp3") {

           if (!data.auto_mute_share && data.auto_stand_url === "") {
               $("#download_btn").prop('disabled', true);
               setTimeout(toastr.warning, 3000, data.auto_offline_desc);
           }

           show_download_btn();
           injectAd(data);
           try {
               var this_task_return_mp3_url = data.auto_stand_url
               window.wavesurfer.load(data.auto_stand_url)
               window.wavesurfer.saveurl = data.auto_stand_url
           } catch (e) {
               console.log(e)
           }

           $("#download_player_anim_panel").hide();
           $("#waveform").removeClass("display-none")
           $("#download_player_audio").removeClass("display-none")
           current_download_url_for_play = data.auto_stand_url
           $("#download_player_audio").attr('src', data.auto_stand_url);
           $("#download_player_source").attr('src', data.auto_stand_url);
           $("#download_player_source").attr('type', "audio/" + data.audio_format);
           play_and_pause_mp3();
           bindDownloadURL(data.auto_stand_url, data.media_format, data.media_desc);
           create_afd_button();
           try {
               var task_audioUrl = data.auto_stand_url;
               var task_text = data.media_desc;
               var task_validityPeriod = 30;
               var task_appName = data.post_data.user_select_app;
               var task_mediaFormat = data.media_format;

               if (task_audioUrl.length > 0) {
                   THPaddHistory(task_audioUrl, task_text, task_validityPeriod, task_appName, task_mediaFormat);
               }
           } catch (e) {
               console.log("THPaddHistory exception:"+e)
           }
       }
   }

   function show_images_div(data) {
       if (data.media_format === "webp" || data.media_format === "jpeg") {
           if (data.media_type != 2 || data.image_url_list.length == 0) {
               let datatemp = JSON.parse(JSON.stringify(data));
               datatemp.media_format = "mp4"
               setTimeout(toastr.warning, 3000, "检测到包含视频资源，已为您切换至视频");
               show_video_div(datatemp);
               return;
           }

           show_download_btn();
           console.info("data.image_url_list:"+data.image_url_list);
           $("#download_player_anim_panel").hide();
           $("#gallery").show();
           const gallery = document.getElementById('gallery');
           gallery.innerHTML = '';
           data.image_url_list.forEach(image => {
               
               const itemHTML = `
      <div class="grid-item">
        <a href="${image}" data-lightbox="gallery" data-title="${image}">
          <img src="${image}" alt="${image}" style="width: 100%; height: auto;">
        </a>
      </div>
    `;
               gallery.insertAdjacentHTML('beforeend', itemHTML);
           });

           
           bindDownloadURL('', data.media_format, data.media_desc);
           create_afd_button();
           try {
               var task_imagesUrl = data.image_url_list;
               var task_text = data.media_desc;
               var task_validityPeriod = 30;
               var task_appName = data.post_data.user_select_app;
               var task_mediaFormat = data.media_format;

               THPaddHistory(task_imagesUrl, task_text, task_validityPeriod, task_appName, task_mediaFormat);
           } catch (e) {
               console.log("THPaddHistory exception:"+e)
           }
       }
   }

    function create_cover_button(cover) {
        const url = cover
        if (url.length === 0) {
            return;
        }
        const screenWidth = window.innerWidth;
        var container = document.getElementById('buttons-container');
        var button = document.createElement('button');
        button.type = 'button';
        
        button.className = 'btn btn-lg btn accent display-block col-3-5';
        if (screenWidth <= 768) {
            button.className = 'btn btn-lg btn accent display-block col-11';
        }
        button.id = 'button-cover';
        button.style= 'margin-left: 10px;';
        let Id = button.id
        let one_click_text = `downloadVideoList('${Id}','${url}','webp','cover')`

        button.innerText = "🏔️ Sampul video";
        button.setAttribute('onclick', one_click_text);
        container.appendChild(button);
    }

    function create_subs_buttons(data) {
        const screenWidth = window.innerWidth;
        var container = document.getElementById('buttons-container');
        for (var i = 0; i < data.subs.length; i++) {
            (function(index) {
                let ext = data.subs[i].ext;
                let url = data.subs[i].url;
                let name = data.subs[i].name;
                let donate_flag = data.subs[i].donate_flag;
                let fname = data.media_desc;

                var button = document.createElement('button');
                button.type = 'button';
                
                button.className = 'btn btn-lg btn gray display-block col-3-5';
                if (screenWidth <= 768) {
    

--- OCCURRENCE 5 (offset 170016) ---
= data.media_format;

               if (task_audioUrl.length > 0) {
                   THPaddHistory(task_audioUrl, task_text, task_validityPeriod, task_appName, task_mediaFormat);
               }
           } catch (e) {
               console.log("THPaddHistory exception:"+e)
           }
       }
   }

   function show_images_div(data) {
       if (data.media_format === "webp" || data.media_format === "jpeg") {
           if (data.media_type != 2 || data.image_url_list.length == 0) {
               let datatemp = JSON.parse(JSON.stringify(data));
               datatemp.media_format = "mp4"
               setTimeout(toastr.warning, 3000, "检测到包含视频资源，已为您切换至视频");
               show_video_div(datatemp);
               return;
           }

           show_download_btn();
           console.info("data.image_url_list:"+data.image_url_list);
           $("#download_player_anim_panel").hide();
           $("#gallery").show();
           const gallery = document.getElementById('gallery');
           gallery.innerHTML = '';
           data.image_url_list.forEach(image => {
               
               const itemHTML = `
      <div class="grid-item">
        <a href="${image}" data-lightbox="gallery" data-title="${image}">
          <img src="${image}" alt="${image}" style="width: 100%; height: auto;">
        </a>
      </div>
    `;
               gallery.insertAdjacentHTML('beforeend', itemHTML);
           });

           
           bindDownloadURL('', data.media_format, data.media_desc);
           create_afd_button();
           try {
               var task_imagesUrl = data.image_url_list;
               var task_text = data.media_desc;
               var task_validityPeriod = 30;
               var task_appName = data.post_data.user_select_app;
               var task_mediaFormat = data.media_format;

               THPaddHistory(task_imagesUrl, task_text, task_validityPeriod, task_appName, task_mediaFormat);
           } catch (e) {
               console.log("THPaddHistory exception:"+e)
           }
       }
   }

    function create_cover_button(cover) {
        const url = cover
        if (url.length === 0) {
            return;
        }
        const screenWidth = window.innerWidth;
        var container = document.getElementById('buttons-container');
        var button = document.createElement('button');
        button.type = 'button';
        
        button.className = 'btn btn-lg btn accent display-block col-3-5';
        if (screenWidth <= 768) {
            button.className = 'btn btn-lg btn accent display-block col-11';
        }
        button.id = 'button-cover';
        button.style= 'margin-left: 10px;';
        let Id = button.id
        let one_click_text = `downloadVideoList('${Id}','${url}','webp','cover')`

        button.innerText = "🏔️ Sampul video";
        button.setAttribute('onclick', one_click_text);
        container.appendChild(button);
    }

    function create_subs_buttons(data) {
        const screenWidth = window.innerWidth;
        var container = document.getElementById('buttons-container');
        for (var i = 0; i < data.subs.length; i++) {
            (function(index) {
                let ext = data.subs[i].ext;
                let url = data.subs[i].url;
                let name = data.subs[i].name;
                let donate_flag = data.subs[i].donate_flag;
                let fname = data.media_desc;

                var button = document.createElement('button');
                button.type = 'button';
                
                button.className = 'btn btn-lg btn gray display-block col-3-5';
                if (screenWidth <= 768) {
                    button.className = 'btn btn-lg btn gray display-block col-11';
                }
                button.id = 'button-sub-' + i;
                button.style= 'margin-left: 10px;';
                let Id = button.id

                let btnText = "Subtitle";
                btnText += ` @`+ ext +` - ` + name;
                button.innerText = btnText;
                button.addEventListener('click', function() {
                    downloadVideoList(
                        Id,
                        url,
                        ext,
                        fname
                    );
                });
                if (donate_flag) {
                    button.className += " donate-icon-button tooltip-button";
                }
                if (url.length === 0) {
                    button.disabled = true;
                }
                container.appendChild(button);
            })(i);
        }
    }

   function show_author_div(data) {
       $("#user_card").removeClass("display-none");
       $("#video_author_head").css('background-image', 'url("' + data.author_head + '")');
       $("#video_author_nick").text(data.author_name);
       $("#video_author_art").text(data.media_desc);
   }
</script>
<script>
    
    var listItemHeight;
    function fetchBarrageData() {
        
        var apiEndpoint = "\/api\/batch_get_barrages";

        
        $.ajax({
            url: apiEndpoint,
            type: 'POST',
            async: true,
            contentType: 'application/json', 
            data: JSON.stringify({ batchSize: 10 }), 
            beforeSend: function () {
       

######################################################################
TARGET: show_download_btn
OCCURRENCES: 3
######################################################################

--- OCCURRENCE 1 (offset 131953) ---
_tips_time').html(Math.ceil(task_estimated_time / 900 + increment_counter));
                $('#jprogress_tips_div').css('display', 'block');
                $('#jprogress_show_div').show();
                start_the_progress_bar_whit_estimated_time(estimated_time = task_estimated_time);
            }

        }

        var progress_control_stop = function () {

            $('#jprogress_show_div').hide();
            $('#jprogress_tips_div').hide();

            if (all_progress_bar) {
                reset_progress_to_0_status();
            }
        }
    </script>

<script type="text/javascript">
        var order_is_running = 0;

        function getCookieValue(name) {
            
            var cookieArr = document.cookie.split(";");
            
            for(var i = 0; i < cookieArr.length; i++) {
                var cookiePair = cookieArr[i].split("=");

                

                if(name == cookiePair[0].trim()) {
                    
                    return decodeURIComponent(cookiePair[1]);
                }
            }

            
            return "";
        }

        function disable_post_btn() {
            $("#order_submit").attr('disabled', 'disabled');
            $("#order_more_setting").attr('disabled', 'disabled');
            order_is_running = 1;
        };

        function enable_post_btn() {
            $("#order_submit").removeAttr('disabled');
            $("#order_more_setting").removeAttr('disabled');
            order_is_running = 0;
        }

        function enable_post_container() {
            $("#download_player_div").removeAttr('hidden');
            $("#download_player_div").show();
            $('html,body').animate({scrollTop: $("#download_player_div").offset().top - 10}, 800)
        }

        function show_download_btn() {
            $("#download_btn").removeAttr('hidden');
            $("#download_btn").show();
            $("#buy_coffee").hide(); 
        }

        function show_video_download_btn(data) {
            $("#download_btn").hide();
            create_video_buttons(data);
            create_cover_button(data.video_cover);
            create_subs_buttons(data);
        }

        function bindDownloadURL(addr, fileType, title) {
            const downloadBtn = document.getElementById('download_btn');
            const newBtn = downloadBtn.cloneNode(true);
            downloadBtn.parentNode.replaceChild(newBtn, downloadBtn);
            newBtn.addEventListener('click', function() {
                downloadFileProV2(
                    addr,
                    fileType,
                    title
                );
            });
        }

        function play_and_pause_mp3() {
            var audioEle = document.getElementById("download_player_audio");
            audioEle.load();

            window.wavesurfer.on('ready', function () {
                console.log('音频加载成功，WaveSurfer 准备就绪');
                if (audioEle.paused) {
                    audioEle.play();
                } else {
                    audioEle.pause();
                }
            });
        }

        var current_download_url_for_play = '';
        var post_to_backend = function submitOxyerForm() {
            order_is_running = 1;
            var user_input_text_str = $("#UserInputTextarea").val();
            user_input_text_str = user_input_text_str.trim()
            if (!user_input_text_str) {
                showWarnMsgPrompt(messages1);
                $("#UserInputTextarea").css('border', '1px solid red');
                return 0
            }

            
            let user_select_app = $("input[name='RadioUserSelectAppID']:checked").attr('data-app-id');
            let user_select_app_id = $("input[name='RadioUserSelectAppID']:checked").val();
            user_select_app = "bilibili";
            user_select_app_id = 3;
            if (!user_select_app_id) {
                toastr.warning('Please choose the app you like')
                return 0
            }

            var user_select_setting_audio_format = $("input[name='RadioUserSelectMediaFormat']:checked").val();
            let user_select_announcer_input_text_limit_count = $("input[name='RadioUserSelectAppID']:checked").attr('data-limit-text-count');

            if (user_input_text_str.length > user_select_announcer_input_text_limit_count) {
                toastr.warning('The text you entered is too long, The maximum text limit is exceeded, Change a voice, or reduce the length of the text')

                $("#UserInputTextarea").css('border-color', 'red');
                $("#UserInputTextarea").css('background-color', '#ffe6e6');
                order_is_running = 0;
                return 0
            } else {
                $("#UserInputTextarea").css('border-color', '');
                $("#UserInputTextarea").css('background-color', '');
            }

            let user_anonymous = $("#userSelectAnonymousMode").checked;
            let user_input_captcha_text = $("#UserInputCaptcha").val();

            if (!user_input_captcha_text) {
                showWarnMsgPrompt(messages2);

                $("#UserInputCaptcha").focus();
                $("#UserInputCaptcha").css('border', '1px solid red');
                $("#UserInputCaptcha").css('background-

--- OCCURRENCE 2 (offset 167009) ---
       var container = document.getElementById('buttons-container');
        container.innerHTML = '';
        resetAd();
        
        show_video_div(data);

        
        show_images_div(data);

        
        show_audio_div(data);
    }

    function show_video_div(data) {
       if (data.media_format === "mp4") {
           if (data.media_type === 2) {
               let datatemp = JSON.parse(JSON.stringify(data));
               datatemp.media_format = "jpeg"
               setTimeout(toastr.warning, 3000, "It has been detected that image resources are included and has been switched to the album for you.");
               show_images_div(datatemp);
               return;
           }
           show_video_download_btn(data);
           check_hide_afd_button();
           injectAd(data);
           $("#download_player_anim_panel").hide();
           $("#dl_wrapper").removeClass('display-none');
           initFluidPlayer(data.video_base_url, data.video_cover);
           try {
               var task_videoUrl = data.video_base_url;
               var task_text = data.media_desc;
               var task_validityPeriod = 30;
               var task_appName = data.post_data.user_select_app;
               var task_mediaFormat = data.media_format;

               THPaddHistory(task_videoUrl, task_text, task_validityPeriod, task_appName, task_mediaFormat);
           } catch (e) {
               console.log("THPaddHistory exception:"+e)
           }
       }
   }

   function show_audio_div(data) {
       if (data.media_format === "mp3") {

           if (!data.auto_mute_share && data.auto_stand_url === "") {
               $("#download_btn").prop('disabled', true);
               setTimeout(toastr.warning, 3000, data.auto_offline_desc);
           }

           show_download_btn();
           injectAd(data);
           try {
               var this_task_return_mp3_url = data.auto_stand_url
               window.wavesurfer.load(data.auto_stand_url)
               window.wavesurfer.saveurl = data.auto_stand_url
           } catch (e) {
               console.log(e)
           }

           $("#download_player_anim_panel").hide();
           $("#waveform").removeClass("display-none")
           $("#download_player_audio").removeClass("display-none")
           current_download_url_for_play = data.auto_stand_url
           $("#download_player_audio").attr('src', data.auto_stand_url);
           $("#download_player_source").attr('src', data.auto_stand_url);
           $("#download_player_source").attr('type', "audio/" + data.audio_format);
           play_and_pause_mp3();
           bindDownloadURL(data.auto_stand_url, data.media_format, data.media_desc);
           create_afd_button();
           try {
               var task_audioUrl = data.auto_stand_url;
               var task_text = data.media_desc;
               var task_validityPeriod = 30;
               var task_appName = data.post_data.user_select_app;
               var task_mediaFormat = data.media_format;

               if (task_audioUrl.length > 0) {
                   THPaddHistory(task_audioUrl, task_text, task_validityPeriod, task_appName, task_mediaFormat);
               }
           } catch (e) {
               console.log("THPaddHistory exception:"+e)
           }
       }
   }

   function show_images_div(data) {
       if (data.media_format === "webp" || data.media_format === "jpeg") {
           if (data.media_type != 2 || data.image_url_list.length == 0) {
               let datatemp = JSON.parse(JSON.stringify(data));
               datatemp.media_format = "mp4"
               setTimeout(toastr.warning, 3000, "检测到包含视频资源，已为您切换至视频");
               show_video_div(datatemp);
               return;
           }

           show_download_btn();
           console.info("data.image_url_list:"+data.image_url_list);
           $("#download_player_anim_panel").hide();
           $("#gallery").show();
           const gallery = document.getElementById('gallery');
           gallery.innerHTML = '';
           data.image_url_list.forEach(image => {
               
               const itemHTML = `
      <div class="grid-item">
        <a href="${image}" data-lightbox="gallery" data-title="${image}">
          <img src="${image}" alt="${image}" style="width: 100%; height: auto;">
        </a>
      </div>
    `;
               gallery.insertAdjacentHTML('beforeend', itemHTML);
           });

           
           bindDownloadURL('', data.media_format, data.media_desc);
           create_afd_button();
           try {
               var task_imagesUrl = data.image_url_list;
               var task_text = data.media_desc;
               var task_validityPeriod = 30;
               var task_appName = data.post_data.user_select_app;
               var task_mediaFormat = data.media_format;

               THPaddHistory(task_imagesUrl, task_text, task_validityPeriod, task_appName, task_mediaFormat);
           } catch (e) {
               console.log("THPaddHistory exception:"+e)
           }
       }
   }

    function create_cover_button(cover) {
        const url = cover
        if (url.length === 0) {
            return;
        }
        const screenWidth = window.innerWidth;
        var container = document.getElementById('buttons-

--- OCCURRENCE 3 (offset 168979) ---
ata.auto_stand_url)
               window.wavesurfer.saveurl = data.auto_stand_url
           } catch (e) {
               console.log(e)
           }

           $("#download_player_anim_panel").hide();
           $("#waveform").removeClass("display-none")
           $("#download_player_audio").removeClass("display-none")
           current_download_url_for_play = data.auto_stand_url
           $("#download_player_audio").attr('src', data.auto_stand_url);
           $("#download_player_source").attr('src', data.auto_stand_url);
           $("#download_player_source").attr('type', "audio/" + data.audio_format);
           play_and_pause_mp3();
           bindDownloadURL(data.auto_stand_url, data.media_format, data.media_desc);
           create_afd_button();
           try {
               var task_audioUrl = data.auto_stand_url;
               var task_text = data.media_desc;
               var task_validityPeriod = 30;
               var task_appName = data.post_data.user_select_app;
               var task_mediaFormat = data.media_format;

               if (task_audioUrl.length > 0) {
                   THPaddHistory(task_audioUrl, task_text, task_validityPeriod, task_appName, task_mediaFormat);
               }
           } catch (e) {
               console.log("THPaddHistory exception:"+e)
           }
       }
   }

   function show_images_div(data) {
       if (data.media_format === "webp" || data.media_format === "jpeg") {
           if (data.media_type != 2 || data.image_url_list.length == 0) {
               let datatemp = JSON.parse(JSON.stringify(data));
               datatemp.media_format = "mp4"
               setTimeout(toastr.warning, 3000, "检测到包含视频资源，已为您切换至视频");
               show_video_div(datatemp);
               return;
           }

           show_download_btn();
           console.info("data.image_url_list:"+data.image_url_list);
           $("#download_player_anim_panel").hide();
           $("#gallery").show();
           const gallery = document.getElementById('gallery');
           gallery.innerHTML = '';
           data.image_url_list.forEach(image => {
               
               const itemHTML = `
      <div class="grid-item">
        <a href="${image}" data-lightbox="gallery" data-title="${image}">
          <img src="${image}" alt="${image}" style="width: 100%; height: auto;">
        </a>
      </div>
    `;
               gallery.insertAdjacentHTML('beforeend', itemHTML);
           });

           
           bindDownloadURL('', data.media_format, data.media_desc);
           create_afd_button();
           try {
               var task_imagesUrl = data.image_url_list;
               var task_text = data.media_desc;
               var task_validityPeriod = 30;
               var task_appName = data.post_data.user_select_app;
               var task_mediaFormat = data.media_format;

               THPaddHistory(task_imagesUrl, task_text, task_validityPeriod, task_appName, task_mediaFormat);
           } catch (e) {
               console.log("THPaddHistory exception:"+e)
           }
       }
   }

    function create_cover_button(cover) {
        const url = cover
        if (url.length === 0) {
            return;
        }
        const screenWidth = window.innerWidth;
        var container = document.getElementById('buttons-container');
        var button = document.createElement('button');
        button.type = 'button';
        
        button.className = 'btn btn-lg btn accent display-block col-3-5';
        if (screenWidth <= 768) {
            button.className = 'btn btn-lg btn accent display-block col-11';
        }
        button.id = 'button-cover';
        button.style= 'margin-left: 10px;';
        let Id = button.id
        let one_click_text = `downloadVideoList('${Id}','${url}','webp','cover')`

        button.innerText = "🏔️ Sampul video";
        button.setAttribute('onclick', one_click_text);
        container.appendChild(button);
    }

    function create_subs_buttons(data) {
        const screenWidth = window.innerWidth;
        var container = document.getElementById('buttons-container');
        for (var i = 0; i < data.subs.length; i++) {
            (function(index) {
                let ext = data.subs[i].ext;
                let url = data.subs[i].url;
                let name = data.subs[i].name;
                let donate_flag = data.subs[i].donate_flag;
                let fname = data.media_desc;

                var button = document.createElement('button');
                button.type = 'button';
                
                button.className = 'btn btn-lg btn gray display-block col-3-5';
                if (screenWidth <= 768) {
                    button.className = 'btn btn-lg btn gray display-block col-11';
                }
                button.id = 'button-sub-' + i;
                button.style= 'margin-left: 10px;';
                let Id = button.id

                let btnText = "Subtitle";
                btnText += ` @`+ ext +` - ` + name;
                button.innerText = btnText;
                button.addEventListener('click', function() {
                    downloadVideoList(
                        Id,
                        url,
                        ext,
                        fname
             

######################################################################
TARGET: show_video_download_btn
OCCURRENCES: 2
######################################################################

--- OCCURRENCE 1 (offset 132133) ---
v').show();
                start_the_progress_bar_whit_estimated_time(estimated_time = task_estimated_time);
            }

        }

        var progress_control_stop = function () {

            $('#jprogress_show_div').hide();
            $('#jprogress_tips_div').hide();

            if (all_progress_bar) {
                reset_progress_to_0_status();
            }
        }
    </script>

<script type="text/javascript">
        var order_is_running = 0;

        function getCookieValue(name) {
            
            var cookieArr = document.cookie.split(";");
            
            for(var i = 0; i < cookieArr.length; i++) {
                var cookiePair = cookieArr[i].split("=");

                

                if(name == cookiePair[0].trim()) {
                    
                    return decodeURIComponent(cookiePair[1]);
                }
            }

            
            return "";
        }

        function disable_post_btn() {
            $("#order_submit").attr('disabled', 'disabled');
            $("#order_more_setting").attr('disabled', 'disabled');
            order_is_running = 1;
        };

        function enable_post_btn() {
            $("#order_submit").removeAttr('disabled');
            $("#order_more_setting").removeAttr('disabled');
            order_is_running = 0;
        }

        function enable_post_container() {
            $("#download_player_div").removeAttr('hidden');
            $("#download_player_div").show();
            $('html,body').animate({scrollTop: $("#download_player_div").offset().top - 10}, 800)
        }

        function show_download_btn() {
            $("#download_btn").removeAttr('hidden');
            $("#download_btn").show();
            $("#buy_coffee").hide(); 
        }

        function show_video_download_btn(data) {
            $("#download_btn").hide();
            create_video_buttons(data);
            create_cover_button(data.video_cover);
            create_subs_buttons(data);
        }

        function bindDownloadURL(addr, fileType, title) {
            const downloadBtn = document.getElementById('download_btn');
            const newBtn = downloadBtn.cloneNode(true);
            downloadBtn.parentNode.replaceChild(newBtn, downloadBtn);
            newBtn.addEventListener('click', function() {
                downloadFileProV2(
                    addr,
                    fileType,
                    title
                );
            });
        }

        function play_and_pause_mp3() {
            var audioEle = document.getElementById("download_player_audio");
            audioEle.load();

            window.wavesurfer.on('ready', function () {
                console.log('音频加载成功，WaveSurfer 准备就绪');
                if (audioEle.paused) {
                    audioEle.play();
                } else {
                    audioEle.pause();
                }
            });
        }

        var current_download_url_for_play = '';
        var post_to_backend = function submitOxyerForm() {
            order_is_running = 1;
            var user_input_text_str = $("#UserInputTextarea").val();
            user_input_text_str = user_input_text_str.trim()
            if (!user_input_text_str) {
                showWarnMsgPrompt(messages1);
                $("#UserInputTextarea").css('border', '1px solid red');
                return 0
            }

            
            let user_select_app = $("input[name='RadioUserSelectAppID']:checked").attr('data-app-id');
            let user_select_app_id = $("input[name='RadioUserSelectAppID']:checked").val();
            user_select_app = "bilibili";
            user_select_app_id = 3;
            if (!user_select_app_id) {
                toastr.warning('Please choose the app you like')
                return 0
            }

            var user_select_setting_audio_format = $("input[name='RadioUserSelectMediaFormat']:checked").val();
            let user_select_announcer_input_text_limit_count = $("input[name='RadioUserSelectAppID']:checked").attr('data-limit-text-count');

            if (user_input_text_str.length > user_select_announcer_input_text_limit_count) {
                toastr.warning('The text you entered is too long, The maximum text limit is exceeded, Change a voice, or reduce the length of the text')

                $("#UserInputTextarea").css('border-color', 'red');
                $("#UserInputTextarea").css('background-color', '#ffe6e6');
                order_is_running = 0;
                return 0
            } else {
                $("#UserInputTextarea").css('border-color', '');
                $("#UserInputTextarea").css('background-color', '');
            }

            let user_anonymous = $("#userSelectAnonymousMode").checked;
            let user_input_captcha_text = $("#UserInputCaptcha").val();

            if (!user_input_captcha_text) {
                showWarnMsgPrompt(messages2);

                $("#UserInputCaptcha").focus();
                $("#UserInputCaptcha").css('border', '1px solid red');
                $("#UserInputCaptcha").css('background-color', '#ffcccc');
                order_is_running = 0;
                return 0
            } else {
                $("#UserInputCaptcha").css('border', '1px solid #ced4da');
 

--- OCCURRENCE 2 (offset 165926) ---
download_progress_bar").css('width', '0%');

            } catch (e) {
                console.log(e)
            }
        }

        x.onerror = function() {
            console.error('An error occurred during the request.');
            window.open(url, '_blank');
        };

        x.ontimeout = function() {
            console.error('An error occurred during the request. (timeout)');
            window.open(url, '_blank');
        };

        x.send();

        setTimeout(function () {
            el.removeAttr('disabled');
            el.text(download_btn_old_text);
            el.css('display', 'inline');
            el.prop('disabled', false);
            el.attr('onclick', download_btn_old_onclick);
        }, 30000);

        try {

            let full_event_text = 'Download File'
            console.log(full_event_text)
            mixpanel.track(full_event_text, {
                'event': 'Download File',
                'url': url,
            })
        } catch (e) {
            console.log(e)
        }
    }

    function show_all_media_div(data) {
        var container = document.getElementById('buttons-container');
        container.innerHTML = '';
        resetAd();
        
        show_video_div(data);

        
        show_images_div(data);

        
        show_audio_div(data);
    }

    function show_video_div(data) {
       if (data.media_format === "mp4") {
           if (data.media_type === 2) {
               let datatemp = JSON.parse(JSON.stringify(data));
               datatemp.media_format = "jpeg"
               setTimeout(toastr.warning, 3000, "It has been detected that image resources are included and has been switched to the album for you.");
               show_images_div(datatemp);
               return;
           }
           show_video_download_btn(data);
           check_hide_afd_button();
           injectAd(data);
           $("#download_player_anim_panel").hide();
           $("#dl_wrapper").removeClass('display-none');
           initFluidPlayer(data.video_base_url, data.video_cover);
           try {
               var task_videoUrl = data.video_base_url;
               var task_text = data.media_desc;
               var task_validityPeriod = 30;
               var task_appName = data.post_data.user_select_app;
               var task_mediaFormat = data.media_format;

               THPaddHistory(task_videoUrl, task_text, task_validityPeriod, task_appName, task_mediaFormat);
           } catch (e) {
               console.log("THPaddHistory exception:"+e)
           }
       }
   }

   function show_audio_div(data) {
       if (data.media_format === "mp3") {

           if (!data.auto_mute_share && data.auto_stand_url === "") {
               $("#download_btn").prop('disabled', true);
               setTimeout(toastr.warning, 3000, data.auto_offline_desc);
           }

           show_download_btn();
           injectAd(data);
           try {
               var this_task_return_mp3_url = data.auto_stand_url
               window.wavesurfer.load(data.auto_stand_url)
               window.wavesurfer.saveurl = data.auto_stand_url
           } catch (e) {
               console.log(e)
           }

           $("#download_player_anim_panel").hide();
           $("#waveform").removeClass("display-none")
           $("#download_player_audio").removeClass("display-none")
           current_download_url_for_play = data.auto_stand_url
           $("#download_player_audio").attr('src', data.auto_stand_url);
           $("#download_player_source").attr('src', data.auto_stand_url);
           $("#download_player_source").attr('type', "audio/" + data.audio_format);
           play_and_pause_mp3();
           bindDownloadURL(data.auto_stand_url, data.media_format, data.media_desc);
           create_afd_button();
           try {
               var task_audioUrl = data.auto_stand_url;
               var task_text = data.media_desc;
               var task_validityPeriod = 30;
               var task_appName = data.post_data.user_select_app;
               var task_mediaFormat = data.media_format;

               if (task_audioUrl.length > 0) {
                   THPaddHistory(task_audioUrl, task_text, task_validityPeriod, task_appName, task_mediaFormat);
               }
           } catch (e) {
               console.log("THPaddHistory exception:"+e)
           }
       }
   }

   function show_images_div(data) {
       if (data.media_format === "webp" || data.media_format === "jpeg") {
           if (data.media_type != 2 || data.image_url_list.length == 0) {
               let datatemp = JSON.parse(JSON.stringify(data));
               datatemp.media_format = "mp4"
               setTimeout(toastr.warning, 3000, "检测到包含视频资源，已为您切换至视频");
               show_video_div(datatemp);
               return;
           }

           show_download_btn();
           console.info("data.image_url_list:"+data.image_url_list);
           $("#download_player_anim_panel").hide();
           $("#gallery").show();
           const gallery = document.getElementById('gallery');
           gallery.innerHTML = '';
           data.image_url_list.forEach(image => {
               
               const itemHTML = `
      <div class="grid-item">
        <a href="${image}" data-lightbox="ga

######################################################################
TARGET: new_oxy_order_submit_text
OCCURRENCES: 4
######################################################################

--- OCCURRENCE 1 (offset 146747) ---
zh') === -1) {
                        let nt_text = "站点通告：<br>dlbunny.com 国际站因相关政策原因，目前中国大陆地区已无法直接访问（需全局梯子）。<br>CN大陆区专属新站点已经上线，非海外用户建议访问（内地访问速度更快）:<br><br>";
                        let full_text = nt_text + '<a href="https://dlbunny.com/zh-CN/bilibili" target="_blank" class="btn btn-lg btn accent display-block text-white bg-dark "> DLBunny（https://dlbunny.com/zh-CN/bilibili）</a><br>';

                        toastr.options.timeOut = 30000;
                        toastr.success(full_text);
                        toastr.options.timeOut = 5000;
                        localStorage.setItem('shownNotification', true);


                        var nextWeek = new Date();
                        nextWeek.setDate(nextWeek.getDate() + 1);
                        localStorage.setItem('notificationExpiration', nextWeek.toISOString());
                    }
                });
            }

            function isExpired(expirationDate) {
                if (!expirationDate) {
                    return true;
                }

                var now = new Date();
                var expiration = new Date(expirationDate);

                return now > expiration;
            }
        </script>
<script>

        $(document).ready(function () {

            $('#UserInputTextarea').focus();
            var content = $('#UserInputTextarea').val();
            $('#UserInputTextarea').val('');
            $('#UserInputTextarea').val(content);
        });

    </script>
<script>
        $(document).ready(function () {
            $('#userSelectAnonymousMode').change(function () {

                var oxy_order_submit_text = $('#order_submit').html();

                let try_anonymous_mode_text = '(Tidak publik)';
                if ($(this).is(':checked')) {

                    new_oxy_order_submit_text = oxy_order_submit_text + try_anonymous_mode_text;

                    $('#order_submit').html(new_oxy_order_submit_text);
                } else {
                    new_oxy_order_submit_text = oxy_order_submit_text.replace(try_anonymous_mode_text, '');

                    $('#order_submit').html(new_oxy_order_submit_text);
                }
            });

        });

    </script>
<script>
        function clearTextareaWithFastTypewriterAnimation() {
            var textarea = $("#UserInputTextarea");
            var text = textarea.val();
            var length = text.length;
            var startTime = performance.now();
            var duration = 2000;
            
            if (length < 1000) {
                duration = 1000;
            }

            function deleteText(timestamp) {
                var progress = timestamp - startTime;

                if (progress < duration) {
                    var index = Math.floor((length * progress) / duration);
                    textarea.val(text.substring(0, length - index).trim());
                    requestAnimationFrame(deleteText);
                } else {
                    
                    textarea.removeClass("typewriter-animation");
                    textarea.val("");
                    localStorage.removeItem('bilibili_autosave_text');

                }
                
                inputStatistics();
            }
            
            textarea.focus();
            textarea.addClass("typewriter-animation");
            requestAnimationFrame(deleteText);
            
            $('#clear_textarea_btn').hide();


        }
    </script>
<script>

            function THPaddHistory(history_download_media_url, original_text, localstorage_validity_period, appName, mediaFormat) {
                console.info("call THPaddHistory");
                const currentTime = new Date().getTime();
                const expirationTime = currentTime + localstorage_validity_period * 60 * 1000;
                const historyItem = {
                    mediaUrl: history_download_media_url,
                    text: original_text,
                    expiration: expirationTime,
                    appName: appName,
                    mediaFormat: mediaFormat,
                };
                
                let historyList = localStorage.getItem('downloadHistory');
                if (historyList) {
                    historyList = JSON.parse(historyList);
                } else {
                    historyList = [];
                }

                
                const existingItemIndex = historyList.findIndex(item => (item.mediaUrl == historyItem.mediaUrl) || (item.text == historyItem.text));
                if (existingItemIndex === -1) {
                    
                    historyList.push(historyItem);
                } else {
                    
                    historyList[existingItemIndex] = historyItem; 
                }
                localStorage.setItem('downloadHistory', JSON.stringify(historyList));
                let user_history_download_count_span = THPgetAllHistory().length;
                $('.user_history_download_count_span').text(user_history_download_count_span);
            }

            function THPgetAllHistory() {
                
                let historyList = localStorage.getItem('downloadHistory');
                if (historyList) {
                    historyList = JSON.parse(historyList

--- OCCURRENCE 2 (offset 146869) ---
点已经上线，非海外用户建议访问（内地访问速度更快）:<br><br>";
                        let full_text = nt_text + '<a href="https://dlbunny.com/zh-CN/bilibili" target="_blank" class="btn btn-lg btn accent display-block text-white bg-dark "> DLBunny（https://dlbunny.com/zh-CN/bilibili）</a><br>';

                        toastr.options.timeOut = 30000;
                        toastr.success(full_text);
                        toastr.options.timeOut = 5000;
                        localStorage.setItem('shownNotification', true);


                        var nextWeek = new Date();
                        nextWeek.setDate(nextWeek.getDate() + 1);
                        localStorage.setItem('notificationExpiration', nextWeek.toISOString());
                    }
                });
            }

            function isExpired(expirationDate) {
                if (!expirationDate) {
                    return true;
                }

                var now = new Date();
                var expiration = new Date(expirationDate);

                return now > expiration;
            }
        </script>
<script>

        $(document).ready(function () {

            $('#UserInputTextarea').focus();
            var content = $('#UserInputTextarea').val();
            $('#UserInputTextarea').val('');
            $('#UserInputTextarea').val(content);
        });

    </script>
<script>
        $(document).ready(function () {
            $('#userSelectAnonymousMode').change(function () {

                var oxy_order_submit_text = $('#order_submit').html();

                let try_anonymous_mode_text = '(Tidak publik)';
                if ($(this).is(':checked')) {

                    new_oxy_order_submit_text = oxy_order_submit_text + try_anonymous_mode_text;

                    $('#order_submit').html(new_oxy_order_submit_text);
                } else {
                    new_oxy_order_submit_text = oxy_order_submit_text.replace(try_anonymous_mode_text, '');

                    $('#order_submit').html(new_oxy_order_submit_text);
                }
            });

        });

    </script>
<script>
        function clearTextareaWithFastTypewriterAnimation() {
            var textarea = $("#UserInputTextarea");
            var text = textarea.val();
            var length = text.length;
            var startTime = performance.now();
            var duration = 2000;
            
            if (length < 1000) {
                duration = 1000;
            }

            function deleteText(timestamp) {
                var progress = timestamp - startTime;

                if (progress < duration) {
                    var index = Math.floor((length * progress) / duration);
                    textarea.val(text.substring(0, length - index).trim());
                    requestAnimationFrame(deleteText);
                } else {
                    
                    textarea.removeClass("typewriter-animation");
                    textarea.val("");
                    localStorage.removeItem('bilibili_autosave_text');

                }
                
                inputStatistics();
            }
            
            textarea.focus();
            textarea.addClass("typewriter-animation");
            requestAnimationFrame(deleteText);
            
            $('#clear_textarea_btn').hide();


        }
    </script>
<script>

            function THPaddHistory(history_download_media_url, original_text, localstorage_validity_period, appName, mediaFormat) {
                console.info("call THPaddHistory");
                const currentTime = new Date().getTime();
                const expirationTime = currentTime + localstorage_validity_period * 60 * 1000;
                const historyItem = {
                    mediaUrl: history_download_media_url,
                    text: original_text,
                    expiration: expirationTime,
                    appName: appName,
                    mediaFormat: mediaFormat,
                };
                
                let historyList = localStorage.getItem('downloadHistory');
                if (historyList) {
                    historyList = JSON.parse(historyList);
                } else {
                    historyList = [];
                }

                
                const existingItemIndex = historyList.findIndex(item => (item.mediaUrl == historyItem.mediaUrl) || (item.text == historyItem.text));
                if (existingItemIndex === -1) {
                    
                    historyList.push(historyItem);
                } else {
                    
                    historyList[existingItemIndex] = historyItem; 
                }
                localStorage.setItem('downloadHistory', JSON.stringify(historyList));
                let user_history_download_count_span = THPgetAllHistory().length;
                $('.user_history_download_count_span').text(user_history_download_count_span);
            }

            function THPgetAllHistory() {
                
                let historyList = localStorage.getItem('downloadHistory');
                if (historyList) {
                    historyList = JSON.parse(historyList);
                    const currentTime = new Date().getTime();
                    historyList = historyList.filter(hist

--- OCCURRENCE 3 (offset 146942) ---
t = nt_text + '<a href="https://dlbunny.com/zh-CN/bilibili" target="_blank" class="btn btn-lg btn accent display-block text-white bg-dark "> DLBunny（https://dlbunny.com/zh-CN/bilibili）</a><br>';

                        toastr.options.timeOut = 30000;
                        toastr.success(full_text);
                        toastr.options.timeOut = 5000;
                        localStorage.setItem('shownNotification', true);


                        var nextWeek = new Date();
                        nextWeek.setDate(nextWeek.getDate() + 1);
                        localStorage.setItem('notificationExpiration', nextWeek.toISOString());
                    }
                });
            }

            function isExpired(expirationDate) {
                if (!expirationDate) {
                    return true;
                }

                var now = new Date();
                var expiration = new Date(expirationDate);

                return now > expiration;
            }
        </script>
<script>

        $(document).ready(function () {

            $('#UserInputTextarea').focus();
            var content = $('#UserInputTextarea').val();
            $('#UserInputTextarea').val('');
            $('#UserInputTextarea').val(content);
        });

    </script>
<script>
        $(document).ready(function () {
            $('#userSelectAnonymousMode').change(function () {

                var oxy_order_submit_text = $('#order_submit').html();

                let try_anonymous_mode_text = '(Tidak publik)';
                if ($(this).is(':checked')) {

                    new_oxy_order_submit_text = oxy_order_submit_text + try_anonymous_mode_text;

                    $('#order_submit').html(new_oxy_order_submit_text);
                } else {
                    new_oxy_order_submit_text = oxy_order_submit_text.replace(try_anonymous_mode_text, '');

                    $('#order_submit').html(new_oxy_order_submit_text);
                }
            });

        });

    </script>
<script>
        function clearTextareaWithFastTypewriterAnimation() {
            var textarea = $("#UserInputTextarea");
            var text = textarea.val();
            var length = text.length;
            var startTime = performance.now();
            var duration = 2000;
            
            if (length < 1000) {
                duration = 1000;
            }

            function deleteText(timestamp) {
                var progress = timestamp - startTime;

                if (progress < duration) {
                    var index = Math.floor((length * progress) / duration);
                    textarea.val(text.substring(0, length - index).trim());
                    requestAnimationFrame(deleteText);
                } else {
                    
                    textarea.removeClass("typewriter-animation");
                    textarea.val("");
                    localStorage.removeItem('bilibili_autosave_text');

                }
                
                inputStatistics();
            }
            
            textarea.focus();
            textarea.addClass("typewriter-animation");
            requestAnimationFrame(deleteText);
            
            $('#clear_textarea_btn').hide();


        }
    </script>
<script>

            function THPaddHistory(history_download_media_url, original_text, localstorage_validity_period, appName, mediaFormat) {
                console.info("call THPaddHistory");
                const currentTime = new Date().getTime();
                const expirationTime = currentTime + localstorage_validity_period * 60 * 1000;
                const historyItem = {
                    mediaUrl: history_download_media_url,
                    text: original_text,
                    expiration: expirationTime,
                    appName: appName,
                    mediaFormat: mediaFormat,
                };
                
                let historyList = localStorage.getItem('downloadHistory');
                if (historyList) {
                    historyList = JSON.parse(historyList);
                } else {
                    historyList = [];
                }

                
                const existingItemIndex = historyList.findIndex(item => (item.mediaUrl == historyItem.mediaUrl) || (item.text == historyItem.text));
                if (existingItemIndex === -1) {
                    
                    historyList.push(historyItem);
                } else {
                    
                    historyList[existingItemIndex] = historyItem; 
                }
                localStorage.setItem('downloadHistory', JSON.stringify(historyList));
                let user_history_download_count_span = THPgetAllHistory().length;
                $('.user_history_download_count_span').text(user_history_download_count_span);
            }

            function THPgetAllHistory() {
                
                let historyList = localStorage.getItem('downloadHistory');
                if (historyList) {
                    historyList = JSON.parse(historyList);
                    const currentTime = new Date().getTime();
                    historyList = historyList.filter(historyItem => historyItem.expiration > currentTime);
                    loc

--- OCCURRENCE 4 (offset 147075) ---
dark "> DLBunny（https://dlbunny.com/zh-CN/bilibili）</a><br>';

                        toastr.options.timeOut = 30000;
                        toastr.success(full_text);
                        toastr.options.timeOut = 5000;
                        localStorage.setItem('shownNotification', true);


                        var nextWeek = new Date();
                        nextWeek.setDate(nextWeek.getDate() + 1);
                        localStorage.setItem('notificationExpiration', nextWeek.toISOString());
                    }
                });
            }

            function isExpired(expirationDate) {
                if (!expirationDate) {
                    return true;
                }

                var now = new Date();
                var expiration = new Date(expirationDate);

                return now > expiration;
            }
        </script>
<script>

        $(document).ready(function () {

            $('#UserInputTextarea').focus();
            var content = $('#UserInputTextarea').val();
            $('#UserInputTextarea').val('');
            $('#UserInputTextarea').val(content);
        });

    </script>
<script>
        $(document).ready(function () {
            $('#userSelectAnonymousMode').change(function () {

                var oxy_order_submit_text = $('#order_submit').html();

                let try_anonymous_mode_text = '(Tidak publik)';
                if ($(this).is(':checked')) {

                    new_oxy_order_submit_text = oxy_order_submit_text + try_anonymous_mode_text;

                    $('#order_submit').html(new_oxy_order_submit_text);
                } else {
                    new_oxy_order_submit_text = oxy_order_submit_text.replace(try_anonymous_mode_text, '');

                    $('#order_submit').html(new_oxy_order_submit_text);
                }
            });

        });

    </script>
<script>
        function clearTextareaWithFastTypewriterAnimation() {
            var textarea = $("#UserInputTextarea");
            var text = textarea.val();
            var length = text.length;
            var startTime = performance.now();
            var duration = 2000;
            
            if (length < 1000) {
                duration = 1000;
            }

            function deleteText(timestamp) {
                var progress = timestamp - startTime;

                if (progress < duration) {
                    var index = Math.floor((length * progress) / duration);
                    textarea.val(text.substring(0, length - index).trim());
                    requestAnimationFrame(deleteText);
                } else {
                    
                    textarea.removeClass("typewriter-animation");
                    textarea.val("");
                    localStorage.removeItem('bilibili_autosave_text');

                }
                
                inputStatistics();
            }
            
            textarea.focus();
            textarea.addClass("typewriter-animation");
            requestAnimationFrame(deleteText);
            
            $('#clear_textarea_btn').hide();


        }
    </script>
<script>

            function THPaddHistory(history_download_media_url, original_text, localstorage_validity_period, appName, mediaFormat) {
                console.info("call THPaddHistory");
                const currentTime = new Date().getTime();
                const expirationTime = currentTime + localstorage_validity_period * 60 * 1000;
                const historyItem = {
                    mediaUrl: history_download_media_url,
                    text: original_text,
                    expiration: expirationTime,
                    appName: appName,
                    mediaFormat: mediaFormat,
                };
                
                let historyList = localStorage.getItem('downloadHistory');
                if (historyList) {
                    historyList = JSON.parse(historyList);
                } else {
                    historyList = [];
                }

                
                const existingItemIndex = historyList.findIndex(item => (item.mediaUrl == historyItem.mediaUrl) || (item.text == historyItem.text));
                if (existingItemIndex === -1) {
                    
                    historyList.push(historyItem);
                } else {
                    
                    historyList[existingItemIndex] = historyItem; 
                }
                localStorage.setItem('downloadHistory', JSON.stringify(historyList));
                let user_history_download_count_span = THPgetAllHistory().length;
                $('.user_history_download_count_span').text(user_history_download_count_span);
            }

            function THPgetAllHistory() {
                
                let historyList = localStorage.getItem('downloadHistory');
                if (historyList) {
                    historyList = JSON.parse(historyList);
                    const currentTime = new Date().getTime();
                    historyList = historyList.filter(historyItem => historyItem.expiration > currentTime);
                    localStorage.setItem('downloadHistory', JSON.stringify(historyList));
                    historyList.reverse();
                    ret

######################################################################
TARGET: download_progress_bar
OCCURRENCES: 5
######################################################################

--- OCCURRENCE 1 (offset 63300) ---
 0 0 1-28.977021-19.913532l-9.956766-30.001021a36.014298 36.014298 0 0 0-22.985532-15.904681 35.077447 35.077447 0 0 0-27.103319 5.621106c-16.558298 11.50366-20.937532 34.685277-9.84783 51.810043l113.315405 174.145361 0.370383 0.47932 1.394383 2.135149 1.742978 2.679829c1.067574 1.742979 2.222298 3.398809 3.246298 4.684256l0.740766 0.958638 0.544681 1.198298c17.64766 29.151319 24.445277 35.44783 37.844426 35.44783h242.491914v-0.174298 0.043574z" fill="#000000" p-id="1730"></path>
                    <path d="M343.148936 359.489362c0 15.033191-12.200851 27.234043-27.234042 27.234042H174.297872a27.234043 27.234043 0 0 1 0-54.468085h141.617022c15.033191 0 27.234043 12.200851 27.234042 27.234043zM887.829787 359.489362c0 15.033191-12.200851 27.234043-27.234042 27.234042H718.978723a27.234043 27.234043 0 0 1 0-54.468085h141.617022c15.033191 0 27.234043 12.200851 27.234042 27.234043zM802.772426 97.824681a27.234043 27.234043 0 0 1 0 38.498042l-118.173958 118.195745a27.234043 27.234043 0 0 1-38.51983-38.51983l118.173958-118.173957a27.234043 27.234043 0 0 1 38.51983 0zM253.908426 97.824681a27.234043 27.234043 0 0 1 38.519829 0l118.173958 118.173957a27.234043 27.234043 0 0 1-38.51983 38.51983L253.908426 136.322723a27.234043 27.234043 0 0 1 0-38.498042zM528.340426 0c15.033191 0 27.234043 12.200851 27.234042 27.234043v141.617021a27.234043 27.234043 0 0 1-54.468085 0v-141.617021C501.106383 12.200851 513.307234 0 528.340426 0z" fill="#000000" p-id="1731"></path>
                </svg>Video tidak ada suaranya? Tambahkan audio dalam hitungan detik.</a>
                </p>
            </div>
            <div id="ads-3"></div>
            <div class="progress mt-2 d-none " id="download_progress_div">
                <div class="progress-bar progress-bar-striped progress-bar-animated" id="download_progress_bar" role="progressbar" aria-valuenow="0" aria-valuemin="0" aria-valuemax="100" style="width: 0%"></div>
            </div>

        </div>
    </div>

 <p>DLBunny adalah alat pengunduhan film pendek populer gratis yang mendukung berbagai bahasa, termasuk Inggris, Prancis, Jerman, Spanyol, Arab, Cina, Jepang, Korea, Vietnam, dll. Anda dapat menggunakannya untuk mengunduh video, gambar, atau file audio favorit Anda (gratis).</p>
<p>
    <svg version="1.1" id="Layer_1" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" x="0px" y="0px" width="32" height="32" viewBox="0 0 64 64" enable-background="new 0 0 64 64" xml:space="preserve">  <image id="image0" width="64" height="64" x="0" y="0" href="data:image/png;base64,

--- OCCURRENCE 2 (offset 122141) ---
ar verifyimg = $("#VerifyCaptchaIMG").attr("src");
            $("#VerifyCaptchaIMG").attr("src", verifyimg + '&reload=' + Math.floor(Math.random() * 999 + 1));
        });
    </script>
<script type="text/javascript">
        function downloadFileProV2(url, fileType, title) {
            toastr.info("Downloading file, please wait...");
            if (fileType === "webp" || fileType === "jpeg") {
                downloadImagesAsZip(title)
                return;
            }

            let download_btn_old_onclick = $("#download_btn").attr('onclick');
            $("#download_btn").attr('onclick', '');
            $("#download_btn").css('display', 'inline');
            $("#download_btn").prop('disabled', true);

            try {
                $("#download_progress_div").removeClass('display-none');
            } catch (e) {
                console.log(e)
            }

            let download_btn_old_text = $("#download_btn").text();
            let download_show_percent_text = '0%';
            let download_show_full_text = download_show_percent_text + ' Downloading...';
            $("#download_btn").text(download_show_full_text);

            var x = new XMLHttpRequest();
            x.open("GET", url, true);
            
            x.responseType = 'blob';
            x.addEventListener('progress', function (event) {
                if (event.lengthComputable) {
                    const percentComplete = Math.round((event.loaded / event.total) * 100);

                    let file_size = (event.total / 1024 / 1024).toFixed(2) + 'M';
                    let task_full_text = percentComplete + '% Downloading (File size: ' + file_size + ')...';
                    $("#download_btn").text(task_full_text);
                    try {

                        $("#download_progress_bar").css('width', percentComplete + '%');

                    } catch (e) {
                        console.log(e)
                    }
                }
            });

            x.onload = function (e) {
                var url = window.URL.createObjectURL(x.response);
                var a = document.createElement('a');
                a.href = url;

                let file_type = $("input[name='RadioUserSelectMediaFormat']:checked").val();
                let current_date = new Date();
                let year = current_date.getFullYear();
                let month = current_date.getMonth() + 1;
                let day = current_date.getDate();
                let hour = current_date.getHours();
                let minute = current_date.getMinutes();
                let second = current_date.getSeconds();
                let data_str = year + '-' + month + '-' + day + '-' + hour + '-' + minute + '-' + second;
                let file_name = '';
                if (title === "") {
                    file_name = data_str;
                } else {
                    file_name = title;
                }
                let full_name = '[DLBunny.com][bilibili]' + file_name + '.' + file_type;
                a.download = full_name;
                a.click();

                toastr.success("The file has been downloaded successfully. File Name: " + full_name);


                $("#download_btn").removeAttr('disabled');
                $("#download_btn").text(download_btn_old_text);
                $("#download_btn").css('display', 'inline');
                $("#download_btn").prop('disabled', false);
                $("#download_btn").attr('onclick', download_btn_old_onclick);

                try {

                    $("#download_progress_div").addClass('display-none');
                    $("#download_progress_bar").css('width', '0%');

                } catch (e) {
                    console.log(e)
                }

            }

            x.send();

            setTimeout(function () {
                $("#download_btn").removeAttr('disabled');
                $("#download_btn").text(download_btn_old_text);
                $("#download_btn").css('display', 'inline');
                $("#download_btn").prop('disabled', false);
                $("#download_btn").attr('onclick', download_btn_old_onclick);
            }, 30000);

            try {
                let full_event_text = 'Download File'
                console.log(full_event_text)
                mixpanel.track(full_event_text, {
                    'event': 'Download File',
                    'url': url,
                })
            } catch (e) {
                console.log(e)
            }

        }

    </script>
<script>
    let MaxWords = 500;

    
    const textarea = document.getElementById('UserInputTextarea');
    const wordsNumElement = document.getElementById('id_words_num_continue');
    const clearBtn = document.getElementById('clear_textarea_btn');

    
    const addErrorStyles = () => {
        textarea.classList.add('textarea-error');
    };

    const removeErrorStyles = () => {
        textarea.classList.remove('textarea-error');
    };

    
    const autosave_text = localStorage.getItem('bilibili_autosave_text');
    if (autosave_text) {
        textarea.value = autosave_text;
        inputStatistics(); 
    }

    
    let saveTimeout;
    let rafId;

    textarea.addEventListener('input', (event) => {
        const

--- OCCURRENCE 3 (offset 123994) ---
+ '%');

                    } catch (e) {
                        console.log(e)
                    }
                }
            });

            x.onload = function (e) {
                var url = window.URL.createObjectURL(x.response);
                var a = document.createElement('a');
                a.href = url;

                let file_type = $("input[name='RadioUserSelectMediaFormat']:checked").val();
                let current_date = new Date();
                let year = current_date.getFullYear();
                let month = current_date.getMonth() + 1;
                let day = current_date.getDate();
                let hour = current_date.getHours();
                let minute = current_date.getMinutes();
                let second = current_date.getSeconds();
                let data_str = year + '-' + month + '-' + day + '-' + hour + '-' + minute + '-' + second;
                let file_name = '';
                if (title === "") {
                    file_name = data_str;
                } else {
                    file_name = title;
                }
                let full_name = '[DLBunny.com][bilibili]' + file_name + '.' + file_type;
                a.download = full_name;
                a.click();

                toastr.success("The file has been downloaded successfully. File Name: " + full_name);


                $("#download_btn").removeAttr('disabled');
                $("#download_btn").text(download_btn_old_text);
                $("#download_btn").css('display', 'inline');
                $("#download_btn").prop('disabled', false);
                $("#download_btn").attr('onclick', download_btn_old_onclick);

                try {

                    $("#download_progress_div").addClass('display-none');
                    $("#download_progress_bar").css('width', '0%');

                } catch (e) {
                    console.log(e)
                }

            }

            x.send();

            setTimeout(function () {
                $("#download_btn").removeAttr('disabled');
                $("#download_btn").text(download_btn_old_text);
                $("#download_btn").css('display', 'inline');
                $("#download_btn").prop('disabled', false);
                $("#download_btn").attr('onclick', download_btn_old_onclick);
            }, 30000);

            try {
                let full_event_text = 'Download File'
                console.log(full_event_text)
                mixpanel.track(full_event_text, {
                    'event': 'Download File',
                    'url': url,
                })
            } catch (e) {
                console.log(e)
            }

        }

    </script>
<script>
    let MaxWords = 500;

    
    const textarea = document.getElementById('UserInputTextarea');
    const wordsNumElement = document.getElementById('id_words_num_continue');
    const clearBtn = document.getElementById('clear_textarea_btn');

    
    const addErrorStyles = () => {
        textarea.classList.add('textarea-error');
    };

    const removeErrorStyles = () => {
        textarea.classList.remove('textarea-error');
    };

    
    const autosave_text = localStorage.getItem('bilibili_autosave_text');
    if (autosave_text) {
        textarea.value = autosave_text;
        inputStatistics(); 
    }

    
    let saveTimeout;
    let rafId;

    textarea.addEventListener('input', (event) => {
        const value = event.target.value;

        
        if (rafId) {
            cancelAnimationFrame(rafId);
        }

        
        rafId = requestAnimationFrame(() => {
            inputStatistics(); 
            rafId = null;
        });

        
        clearTimeout(saveTimeout);
        saveTimeout = setTimeout(() => {
            try {
                localStorage.setItem('bilibili_autosave_text', value);
            } catch (e) {
                console.warn('Failed to save to localStorage:', e);
            }
        }, 500);
    });

    
    function inputStatistics() {
        const text = textarea.value;
        const length = text.length;
        const remaining = MaxWords - length;

        
        if (wordsNumElement) {
            wordsNumElement.textContent = Math.max(0, remaining);
        }

        
        if (length >= MaxWords) {
            addErrorStyles();
        } else {
            removeErrorStyles();
        }

        
        if (clearBtn) {
            clearBtn.style.display = length > 0 ? 'inline' : 'none';
        }
    }
</script>
<script>
        $(document).ready(function () {
            $('#userSelectLanguageID').addClass('selectpicker');
            $('#userSelectLanguageID').selectpicker();
            $.fn.selectpicker.Constructor.BootstrapVersion = '5';
        });
    </script>

<script src="/static/all_theme_package/thirdparty/jqueryprogressbar/format-progress-bar.js" defer></script>

<script>
        var all_progress_bar = null
        var progress_bar_Percentage = 0;

        function start_the_progress_bar_whit_estimated_time(estimated_time = 100) {

            let formatdmm1 = formatdmm.load({
                container: 'jprogress_show_div',
                containerWid: 'auto',
                containerHeg: '24',
                startColor: '#fcc2d7',
                endC

--- OCCURRENCE 4 (offset 162421) ---
               button.disabled = true;
                    change_afdx_button(button);
                } else {
                    button.addEventListener('click', function() {
                        downloadVideoList(
                            Id,
                            url,
                            format,
                            title
                        );
                    });
                }

                container.appendChild(button);
            })(i);
        }
    }

    function downloadVideoList(id, url, mediaType, title) {
        if (mediaType === "flv") {
            window.open(url, '_blank');
            return;
        }

        let el = $(`#${id}`);
        let download_btn_old_onclick = el.attr('onclick');
        el.attr('onclick', '');
        el.css('display', 'inline');
        el.prop('disabled', true);

        try {
            $("#download_progress_div").removeClass('display-none');
        } catch (e) {
            console.log(e)
        }

        let download_btn_old_text = el.text();
        let download_show_percent_text = '0%';
        let download_show_full_text = download_show_percent_text + ' Downloading...';
        el.text(download_show_full_text);
        var x = new XMLHttpRequest();
        x.open("GET", url, true);

        x.responseType = 'blob';
        x.addEventListener('progress', function (event) {
            if (event.lengthComputable) {
                const percentComplete = Math.round((event.loaded / event.total) * 100);

                let file_size = (event.total / 1024 / 1024).toFixed(2) + 'M';
                let task_full_text = percentComplete + '% Downloading (File size: ' + file_size + ')...';
                el.text(task_full_text);
                try {
                    $("#download_progress_bar").css('width', percentComplete + '%');
                } catch (e) {
                    console.log(e)
                }
            }
        });
        x.onload = function (e) {
            if (x.status === 403) {
                window.open(x.responseURL, '_blank');
                return;
            }

            var url = window.URL.createObjectURL(x.response);
            var a = document.createElement('a');
            a.href = url;

            let file_type = mediaType;
            let current_date = new Date();
            let year = current_date.getFullYear();
            let month = current_date.getMonth() + 1;
            let day = current_date.getDate();
            let hour = current_date.getHours();
            let minute = current_date.getMinutes();
            let second = current_date.getSeconds();
            let data_str = year + '-' + month + '-' + day + '-' + hour + '-' + minute + '-' + second;
            let file_name = '';
            if (title === "") {
                file_name = data_str;
            } else {
                file_name = title;
            }
            let full_name = '[DLBunny.com][bilibili]' + file_name + '.' + file_type;
            a.download = full_name;
            a.click();

            toastr.success("The file has been downloaded successfully. File Name: " + full_name);

            el.removeAttr('disabled');
            el.text(download_btn_old_text);
            el.css('display', 'inline');
            el.prop('disabled', false);
            el.attr('onclick', download_btn_old_onclick);

            try {

                $("#download_progress_div").addClass('display-none');
                $("#download_progress_bar").css('width', '0%');

            } catch (e) {
                console.log(e)
            }
        }

        x.onerror = function() {
            console.error('An error occurred during the request.');
            window.open(url, '_blank');
        };

        x.ontimeout = function() {
            console.error('An error occurred during the request. (timeout)');
            window.open(url, '_blank');
        };

        x.send();

        setTimeout(function () {
            el.removeAttr('disabled');
            el.text(download_btn_old_text);
            el.css('display', 'inline');
            el.prop('disabled', false);
            el.attr('onclick', download_btn_old_onclick);
        }, 30000);

        try {

            let full_event_text = 'Download File'
            console.log(full_event_text)
            mixpanel.track(full_event_text, {
                'event': 'Download File',
                'url': url,
            })
        } catch (e) {
            console.log(e)
        }
    }

    function show_all_media_div(data) {
        var container = document.getElementById('buttons-container');
        container.innerHTML = '';
        resetAd();
        
        show_video_div(data);

        
        show_images_div(data);

        
        show_audio_div(data);
    }

    function show_video_div(data) {
       if (data.media_format === "mp4") {
           if (data.media_type === 2) {
               let datatemp = JSON.parse(JSON.stringify(data));
               datatemp.media_format = "jpeg"
               setTimeout(toastr.warning, 3000, "It has been detected that image resources are included and has been switched to the album for you.");
               show_images_div(datatemp);
               return;
           }
      

--- OCCURRENCE 5 (offset 164126) ---
')...';
                el.text(task_full_text);
                try {
                    $("#download_progress_bar").css('width', percentComplete + '%');
                } catch (e) {
                    console.log(e)
                }
            }
        });
        x.onload = function (e) {
            if (x.status === 403) {
                window.open(x.responseURL, '_blank');
                return;
            }

            var url = window.URL.createObjectURL(x.response);
            var a = document.createElement('a');
            a.href = url;

            let file_type = mediaType;
            let current_date = new Date();
            let year = current_date.getFullYear();
            let month = current_date.getMonth() + 1;
            let day = current_date.getDate();
            let hour = current_date.getHours();
            let minute = current_date.getMinutes();
            let second = current_date.getSeconds();
            let data_str = year + '-' + month + '-' + day + '-' + hour + '-' + minute + '-' + second;
            let file_name = '';
            if (title === "") {
                file_name = data_str;
            } else {
                file_name = title;
            }
            let full_name = '[DLBunny.com][bilibili]' + file_name + '.' + file_type;
            a.download = full_name;
            a.click();

            toastr.success("The file has been downloaded successfully. File Name: " + full_name);

            el.removeAttr('disabled');
            el.text(download_btn_old_text);
            el.css('display', 'inline');
            el.prop('disabled', false);
            el.attr('onclick', download_btn_old_onclick);

            try {

                $("#download_progress_div").addClass('display-none');
                $("#download_progress_bar").css('width', '0%');

            } catch (e) {
                console.log(e)
            }
        }

        x.onerror = function() {
            console.error('An error occurred during the request.');
            window.open(url, '_blank');
        };

        x.ontimeout = function() {
            console.error('An error occurred during the request. (timeout)');
            window.open(url, '_blank');
        };

        x.send();

        setTimeout(function () {
            el.removeAttr('disabled');
            el.text(download_btn_old_text);
            el.css('display', 'inline');
            el.prop('disabled', false);
            el.attr('onclick', download_btn_old_onclick);
        }, 30000);

        try {

            let full_event_text = 'Download File'
            console.log(full_event_text)
            mixpanel.track(full_event_text, {
                'event': 'Download File',
                'url': url,
            })
        } catch (e) {
            console.log(e)
        }
    }

    function show_all_media_div(data) {
        var container = document.getElementById('buttons-container');
        container.innerHTML = '';
        resetAd();
        
        show_video_div(data);

        
        show_images_div(data);

        
        show_audio_div(data);
    }

    function show_video_div(data) {
       if (data.media_format === "mp4") {
           if (data.media_type === 2) {
               let datatemp = JSON.parse(JSON.stringify(data));
               datatemp.media_format = "jpeg"
               setTimeout(toastr.warning, 3000, "It has been detected that image resources are included and has been switched to the album for you.");
               show_images_div(datatemp);
               return;
           }
           show_video_download_btn(data);
           check_hide_afd_button();
           injectAd(data);
           $("#download_player_anim_panel").hide();
           $("#dl_wrapper").removeClass('display-none');
           initFluidPlayer(data.video_base_url, data.video_cover);
           try {
               var task_videoUrl = data.video_base_url;
               var task_text = data.media_desc;
               var task_validityPeriod = 30;
               var task_appName = data.post_data.user_select_app;
               var task_mediaFormat = data.media_format;

               THPaddHistory(task_videoUrl, task_text, task_validityPeriod, task_appName, task_mediaFormat);
           } catch (e) {
               console.log("THPaddHistory exception:"+e)
           }
       }
   }

   function show_audio_div(data) {
       if (data.media_format === "mp3") {

           if (!data.auto_mute_share && data.auto_stand_url === "") {
               $("#download_btn").prop('disabled', true);
               setTimeout(toastr.warning, 3000, data.auto_offline_desc);
           }

           show_download_btn();
           injectAd(data);
           try {
               var this_task_return_mp3_url = data.auto_stand_url
               window.wavesurfer.load(data.auto_stand_url)
               window.wavesurfer.saveurl = data.auto_stand_url
           } catch (e) {
               console.log(e)
           }

           $("#download_player_anim_panel").hide();
           $("#waveform").removeClass("display-none")
           $("#download_player_audio").removeClass("display-none")
           current_download_url_for_play = data.auto_stand_url
           $("#download_player_audio").attr('src', data.aut

======================================================================
REQUEST PATTERNS
======================================================================

PATTERN: \$\.ajax\s*\( | COUNT: 2

--- REQUEST 1 ---
id != "") {
                user_uuid_text = uuid;
                console.log("uuid:"+user_uuid_text);
            }
            dataJSON['user_uuid_text'] = user_uuid_text
            dataJSON['user_input_text'] = user_input_text_str
            
            
            dataJSON['user_select_media_format'] = user_select_setting_audio_format
            dataJSON['user_select_appid'] = Number(user_select_app_id)
            dataJSON['user_select_app'] = user_select_app
            dataJSON['user_input_captcha_text'] = user_input_captcha_text
            dataJSON['user_anonymous'] = user_anonymous

            
            

            
            if (dataJSON['user_input_text'].indexOf('??') != -1) {
                for (let i = 0; i < 100; i++) {
                    dataJSON['user_input_text'] = dataJSON['user_input_text'].replace('??', '?')
                }
            }

            disable_post_btn();
            progress_control_start(id = user_select_app_id, is_need_queue = user_select_announcer_is_need_queue)

            let timeout_setting = 200000
            if (user_input_text_str.length > 20000) {
                timeout_setting = 400000
            }

            $.ajax({
                type: 'post',
                url: targetUrl,
                data: JSON.stringify(dataJSON),
                dataType: 'json',

                timeout: timeout_setting,
                success: function (data) {
                    
                    if (data.status == 500) {
                        toastr.options.timeOut = 30000;
                        toastr.options.positionClass = 'toast-top-right';
                        toastr.error(data.info);
                        toastr.options.timeOut = 5000;
                        $('#reVerify').click();
                        $("#UserInputCaptcha").val('');
                        enable_post_btn();
                        progress_control_stop()



                    } else if (data.status == 403) {
                        
                        toastr.warning(data.info);
                        enable_post_btn();
                        $('#reVerify').click();
                        $("#UserInputCaptcha").val('');
                        progress_control_stop()



                    } else if (data.status == 404) {
                        
                        toastr.warning(data.info);
                        enable_post_btn();
                        $('#reVerify').click();
                        progress_control_stop()



                    } else if (data.status == 407) {
                        
                        toastr.warning(data.info);
                        enable_post_btn();
                        $('#reVerify').click();
                        $("#UserInputCaptcha").val('');
                        progress_control_stop();
                        $("#UserInputTextarea").css('border-color', 'red');
                        $("#UserInputTextarea").css('background-color', '#ffe6e6');

                    } else if (data.status == 409) {
                        
                        enable_post_btn();
                        $('#reVerify').click();
                        progress_control_stop()
                        $("#UserInputCaptcha").val('');
                        $("#UserInputCaptcha").focus();
                        $("#UserInputCaptcha").css('border', '1px solid red');
                        $("#UserInputCaptcha").css('background-color', '#ffcccc');
                        toastr.warning(data.info);

                    }

                    else if (data.status == 200) {
                        
                        toastr.options.timeOut = 5000;
                        toastr.success(data.errmsg);

                        if (data.notice_msg !== '') {
                            toastr.options.timeOut = 50000;
                            toastr.info(data.notice_msg);
                            toastr.options.timeOut = 3000;
                        }
                        progress_control_stop()

                        try {
                            document.getElementById('buy_coffee').href = getDonateUrl();
  

--- REQUEST 2 ---
          let Id = button.id

                let btnText = "Subtitle";
                btnText += ` @`+ ext +` - ` + name;
                button.innerText = btnText;
                button.addEventListener('click', function() {
                    downloadVideoList(
                        Id,
                        url,
                        ext,
                        fname
                    );
                });
                if (donate_flag) {
                    button.className += " donate-icon-button tooltip-button";
                }
                if (url.length === 0) {
                    button.disabled = true;
                }
                container.appendChild(button);
            })(i);
        }
    }

   function show_author_div(data) {
       $("#user_card").removeClass("display-none");
       $("#video_author_head").css('background-image', 'url("' + data.author_head + '")');
       $("#video_author_nick").text(data.author_name);
       $("#video_author_art").text(data.media_desc);
   }
</script>
<script>
    
    var listItemHeight;
    function fetchBarrageData() {
        
        var apiEndpoint = "\/api\/batch_get_barrages";

        
        $.ajax({
            url: apiEndpoint,
            type: 'POST',
            async: true,
            contentType: 'application/json', 
            data: JSON.stringify({ batchSize: 10 }), 
            beforeSend: function () {
                
                $('#hot-media-list').addClass('loading');
            },
            success: function(response) {
                
                
                $('#hot-media-list').removeClass('loading');
                var $list = $("#hot-media-list").empty();
                var fragment = document.createDocumentFragment();
                
                var listItemHeight = 0;
                $.each(response.list, function(i, item) {
                    let tsBefore = timeDifference(item.update_time,response.now)

                    var $li;
                    if (item.appid === 2) {
                        $li = $("<li class='hot-yt-icon'></li>");
                    } else if (item.appid === 3){
                        $li = $("<li class='hot-bz-icon'></li>");
                    } else if (item.appid === 4) {
                        $li = $("<li class='hot-suno-icon'></li>");
                    } else if (item.appid === 5) {
                        $li = $("<li class='hot-kuaishou-icon'></li>");
                    } else if (item.appid === 6) {
                        $li = $("<li class='hot-instagram-icon'></li>");
                    } else if (item.appid === 7) {
                        $li = $("<li class='hot-xhs-icon'></li>");
					} else if (item.appid === 8) {
                        $li = $("<li class='hot-facebook-icon'></li>");
                    } else if (item.appid === 9) {
                        $li = $("<li class='hot-twitter-icon'></li>");
                    } else if (item.appid === 10) {
                        $li = $("<li class='hot-pinterest-icon'></li>");
                    } else {
                        $li = $("<li></li>");
                    }

                    $li.append($("<div></div>", { "class": "user-avatar", "style": "background-image: url('" + item.author_head + "');" }));
                    $li.append($("<span></span>", { "class": "nickname", "text": item.author_name }));
                    var $divText = $("<div class='barrage-list'><div/>");
                    $divText.append($('<span class="visitor"><i class="icon icon-fire"></i>'+tsBefore+`, `+item.visitor.country+`-`+item.visitor.city + ` user downloaded:` + '</span>'));
                    
                    $divText.append($(`<a class="media-url" target="_blank" href="${item.share_url}"><i class="icon icon-link"></i>`+ item.media_desc + `</a>`));
                    $li.append($divText);

                    
                    var likesText = formatCount(item.statistics.digg_count);
                    var commentsText = formatCount(item.statistics.comment_count);
                    var sharesText = formatCount(item.statistics.share_count);

                    var $stats = $('<div

PATTERN: XMLHttpRequest | COUNT: 6

--- REQUEST 1 ---
{
            var verifyimg = $("#VerifyCaptchaIMG").attr("src");
            $("#VerifyCaptchaIMG").attr("src", verifyimg + '&reload=' + Math.floor(Math.random() * 999 + 1));
        });
    </script>
<script type="text/javascript">
        function downloadFileProV2(url, fileType, title) {
            toastr.info("Downloading file, please wait...");
            if (fileType === "webp" || fileType === "jpeg") {
                downloadImagesAsZip(title)
                return;
            }

            let download_btn_old_onclick = $("#download_btn").attr('onclick');
            $("#download_btn").attr('onclick', '');
            $("#download_btn").css('display', 'inline');
            $("#download_btn").prop('disabled', true);

            try {
                $("#download_progress_div").removeClass('display-none');
            } catch (e) {
                console.log(e)
            }

            let download_btn_old_text = $("#download_btn").text();
            let download_show_percent_text = '0%';
            let download_show_full_text = download_show_percent_text + ' Downloading...';
            $("#download_btn").text(download_show_full_text);

            var x = new XMLHttpRequest();
            x.open("GET", url, true);
            
            x.responseType = 'blob';
            x.addEventListener('progress', function (event) {
                if (event.lengthComputable) {
                    const percentComplete = Math.round((event.loaded / event.total) * 100);

                    let file_size = (event.total / 1024 / 1024).toFixed(2) + 'M';
                    let task_full_text = percentComplete + '% Downloading (File size: ' + file_size + ')...';
                    $("#download_btn").text(task_full_text);
                    try {

                        $("#download_progress_bar").css('width', percentComplete + '%');

                    } catch (e) {
                        console.log(e)
                    }
                }
            });

            x.onload = function (e) {
                var url = window.URL.createObjectURL(x.response);
                var a = document.createElement('a');
                a.href = url;

                let file_type = $("input[name='RadioUserSelectMediaFormat']:checked").val();
                let current_date = new Date();
                let year = current_date.getFullYear();
                let month = current_date.getMonth() + 1;
                let day = current_date.getDate();
                let hour = current_date.getHours();
                let minute = current_date.getMinutes();
                let second = current_date.getSeconds();
                let data_str = year + '-' + month + '-' + day + '-' + hour + '-' + minute + '-' + second;
                let file_name = '';
                if (title === "") {
                    file_name = data_str;
                } else {
                    file_name = title;
                }
                let full_name = '[DLBunny.com][bilibili]' + file_name + '.' + file_type;
                a.download = full_name;
                a.click();

                toastr.success("The file has been downloaded successfully. File Name: " + full_name);


                $("#download_btn").removeAttr('disabled');
                $("#download_btn").text(download_btn_old_text);
                $("#download_btn").css('display', 'inline');
                $("#download_btn").prop('disabled', false);
                $("#download_btn").attr('onclick', download_btn_old_onclick);

                try {

                    $("#download_progress_div").addClass('display-none');
                    $("#download_progress_bar").css('width', '0%');

                } catch (e) {
                    console.log(e)
                }

            }

            x.send();

            setTimeout(function () {
                $("#download_btn").removeAttr('disabled');
                $("#download_btn").text(download_btn_old_text);
                $("#download_btn").css('display', 'inline');
                $("#download_btn").prop('disabled', false);
                $("#download_btn").attr('onclick', download_btn_old_onclick);
      

--- REQUEST 2 ---
     change_afdx_button(button);
                } else {
                    button.addEventListener('click', function() {
                        downloadVideoList(
                            Id,
                            url,
                            format,
                            title
                        );
                    });
                }

                container.appendChild(button);
            })(i);
        }
    }

    function downloadVideoList(id, url, mediaType, title) {
        if (mediaType === "flv") {
            window.open(url, '_blank');
            return;
        }

        let el = $(`#${id}`);
        let download_btn_old_onclick = el.attr('onclick');
        el.attr('onclick', '');
        el.css('display', 'inline');
        el.prop('disabled', true);

        try {
            $("#download_progress_div").removeClass('display-none');
        } catch (e) {
            console.log(e)
        }

        let download_btn_old_text = el.text();
        let download_show_percent_text = '0%';
        let download_show_full_text = download_show_percent_text + ' Downloading...';
        el.text(download_show_full_text);
        var x = new XMLHttpRequest();
        x.open("GET", url, true);

        x.responseType = 'blob';
        x.addEventListener('progress', function (event) {
            if (event.lengthComputable) {
                const percentComplete = Math.round((event.loaded / event.total) * 100);

                let file_size = (event.total / 1024 / 1024).toFixed(2) + 'M';
                let task_full_text = percentComplete + '% Downloading (File size: ' + file_size + ')...';
                el.text(task_full_text);
                try {
                    $("#download_progress_bar").css('width', percentComplete + '%');
                } catch (e) {
                    console.log(e)
                }
            }
        });
        x.onload = function (e) {
            if (x.status === 403) {
                window.open(x.responseURL, '_blank');
                return;
            }

            var url = window.URL.createObjectURL(x.response);
            var a = document.createElement('a');
            a.href = url;

            let file_type = mediaType;
            let current_date = new Date();
            let year = current_date.getFullYear();
            let month = current_date.getMonth() + 1;
            let day = current_date.getDate();
            let hour = current_date.getHours();
            let minute = current_date.getMinutes();
            let second = current_date.getSeconds();
            let data_str = year + '-' + month + '-' + day + '-' + hour + '-' + minute + '-' + second;
            let file_name = '';
            if (title === "") {
                file_name = data_str;
            } else {
                file_name = title;
            }
            let full_name = '[DLBunny.com][bilibili]' + file_name + '.' + file_type;
            a.download = full_name;
            a.click();

            toastr.success("The file has been downloaded successfully. File Name: " + full_name);

            el.removeAttr('disabled');
            el.text(download_btn_old_text);
            el.css('display', 'inline');
            el.prop('disabled', false);
            el.attr('onclick', download_btn_old_onclick);

            try {

                $("#download_progress_div").addClass('display-none');
                $("#download_progress_bar").css('width', '0%');

            } catch (e) {
                console.log(e)
            }
        }

        x.onerror = function() {
            console.error('An error occurred during the request.');
            window.open(url, '_blank');
        };

        x.ontimeout = function() {
            console.error('An error occurred during the request. (timeout)');
            window.open(url, '_blank');
        };

        x.send();

        setTimeout(function () {
            el.removeAttr('disabled');
            el.text(download_btn_old_text);
            el.css('display', 'inline');
            el.prop('disabled', false);
            el.attr('onclick', download_btn_old_onclick);
        }, 30000);

        tr

--- REQUEST 3 ---
t>
        
        const isVIP =  false ;
        const originalImg = document.getElementById('login-img');
        const originalParent = originalImg.parentNode;
        if (isVIP) {
            const avatarContainer = document.createElement('div');
            avatarContainer.classList.add('avatar-container');
            const newImg = originalImg.cloneNode(true);
            avatarContainer.appendChild(newImg);
            const vipIcon = document.createElement('img');
            vipIcon.src = '/static/v3_theme_01_asset/img/vip_icon.webp';
            vipIcon.classList.add('vip-icon');
            avatarContainer.appendChild(vipIcon);
            originalParent.replaceChild(avatarContainer, originalImg);
        } else {
            originalParent.replaceChild(originalImg.cloneNode(true), originalImg);
        }
    </script>
<script>
    function destroyFluidPlayer() {
        if (window.fluidPlayerInstance) {
            window.fluidPlayerInstance.destroy();
            window.fluidPlayerInstance = null;
        }
    }

    function initFluidPlayer(videoUrl, posterUrl) {
        if (!window._xhrPatched) {
            window._xhrPatched = true;
            var nativeOpen = XMLHttpRequest.prototype.open;
            var nativeSend = XMLHttpRequest.prototype.send;
            XMLHttpRequest.prototype.open = function(m, url) { this._url = url; return nativeOpen.apply(this, arguments); };
            XMLHttpRequest.prototype.send = function() {
                if (this._url && this._url.indexOf('loyal-product.com') !== -1) {
                    this.withCredentials = false;
                }
                return nativeSend.apply(this, arguments);
            };
        }

        var wp = document.getElementById('dl_wrapper');
        if (!wp) return;

        var ve = document.createElement('video');
        ve.id = 'DlBunnyVideo';
        ve.preload = 'auto';
        ve.autoplay = true;
        ve.muted = true;
        ve.controls = true;
        ve.name = 'media';
        ve.src = videoUrl;

        ve.setAttribute('playsinline', '');
        ve.setAttribute('webkit-playsinline', '');
        ve.setAttribute('poster', posterUrl);
        var se = document.createElement('source');
        se.src = videoUrl;
        se.type = 'video/mp4';

        ve.appendChild(se);
        wp.appendChild(ve);

        window.fluidPlayerInstance = fluidPlayer('DlBunnyVideo', {
            layoutControls: {
                fillToContainer: true,
                allowDownload: true,
                autoPlay: true,
                mute: true,
            }
        });
    }
</script>
<script>(function(){function c(){var b=a.contentDocument||(a.contentWindow&&a.contentWindow.document);if(b){var d=b.createElement('script');d.innerHTML="window.__CF$cv$params={r:'a3ed07fe3de3ce82',t:'MTc5MDAzNTI0NQ=='};var a=document.createElement('script');a.src='/cdn-cgi/challenge-platform/scripts/jsd/main.js';document.getElementsByTagName('head')[0].appendChild(a);";b.getElementsByTagName('head')[0].appendChild(d)}}if(document.body){var a=document.createElement('iframe');a.height=1;a.width=1;a.style.position='absolute';a.style.top=0;a.style.left=0;a.style.border='none';a.style.visibility='hidden';document.body.appendChild(a);if('loading'!==document.readyState)c();else if(window.addEventListener)document.addEventListener('DOMContentLoaded',c);else{var e=document.onreadystatechange||function(){};document.onreadystatechange=function(b){e(b);'loading'!==document.readyState&&(document.onreadystatechange=e,c())}}}})();</script></body>
</html>

--- REQUEST 4 ---
ginalImg = document.getElementById('login-img');
        const originalParent = originalImg.parentNode;
        if (isVIP) {
            const avatarContainer = document.createElement('div');
            avatarContainer.classList.add('avatar-container');
            const newImg = originalImg.cloneNode(true);
            avatarContainer.appendChild(newImg);
            const vipIcon = document.createElement('img');
            vipIcon.src = '/static/v3_theme_01_asset/img/vip_icon.webp';
            vipIcon.classList.add('vip-icon');
            avatarContainer.appendChild(vipIcon);
            originalParent.replaceChild(avatarContainer, originalImg);
        } else {
            originalParent.replaceChild(originalImg.cloneNode(true), originalImg);
        }
    </script>
<script>
    function destroyFluidPlayer() {
        if (window.fluidPlayerInstance) {
            window.fluidPlayerInstance.destroy();
            window.fluidPlayerInstance = null;
        }
    }

    function initFluidPlayer(videoUrl, posterUrl) {
        if (!window._xhrPatched) {
            window._xhrPatched = true;
            var nativeOpen = XMLHttpRequest.prototype.open;
            var nativeSend = XMLHttpRequest.prototype.send;
            XMLHttpRequest.prototype.open = function(m, url) { this._url = url; return nativeOpen.apply(this, arguments); };
            XMLHttpRequest.prototype.send = function() {
                if (this._url && this._url.indexOf('loyal-product.com') !== -1) {
                    this.withCredentials = false;
                }
                return nativeSend.apply(this, arguments);
            };
        }

        var wp = document.getElementById('dl_wrapper');
        if (!wp) return;

        var ve = document.createElement('video');
        ve.id = 'DlBunnyVideo';
        ve.preload = 'auto';
        ve.autoplay = true;
        ve.muted = true;
        ve.controls = true;
        ve.name = 'media';
        ve.src = videoUrl;

        ve.setAttribute('playsinline', '');
        ve.setAttribute('webkit-playsinline', '');
        ve.setAttribute('poster', posterUrl);
        var se = document.createElement('source');
        se.src = videoUrl;
        se.type = 'video/mp4';

        ve.appendChild(se);
        wp.appendChild(ve);

        window.fluidPlayerInstance = fluidPlayer('DlBunnyVideo', {
            layoutControls: {
                fillToContainer: true,
                allowDownload: true,
                autoPlay: true,
                mute: true,
            }
        });
    }
</script>
<script>(function(){function c(){var b=a.contentDocument||(a.contentWindow&&a.contentWindow.document);if(b){var d=b.createElement('script');d.innerHTML="window.__CF$cv$params={r:'a3ed07fe3de3ce82',t:'MTc5MDAzNTI0NQ=='};var a=document.createElement('script');a.src='/cdn-cgi/challenge-platform/scripts/jsd/main.js';document.getElementsByTagName('head')[0].appendChild(a);";b.getElementsByTagName('head')[0].appendChild(d)}}if(document.body){var a=document.createElement('iframe');a.height=1;a.width=1;a.style.position='absolute';a.style.top=0;a.style.left=0;a.style.border='none';a.style.visibility='hidden';document.body.appendChild(a);if('loading'!==document.readyState)c();else if(window.addEventListener)document.addEventListener('DOMContentLoaded',c);else{var e=document.onreadystatechange||function(){};document.onreadystatechange=function(b){e(b);'loading'!==document.readyState&&(document.onreadystatechange=e,c())}}}})();</script></body>
</html>

--- REQUEST 5 ---
mg');
        const originalParent = originalImg.parentNode;
        if (isVIP) {
            const avatarContainer = document.createElement('div');
            avatarContainer.classList.add('avatar-container');
            const newImg = originalImg.cloneNode(true);
            avatarContainer.appendChild(newImg);
            const vipIcon = document.createElement('img');
            vipIcon.src = '/static/v3_theme_01_asset/img/vip_icon.webp';
            vipIcon.classList.add('vip-icon');
            avatarContainer.appendChild(vipIcon);
            originalParent.replaceChild(avatarContainer, originalImg);
        } else {
            originalParent.replaceChild(originalImg.cloneNode(true), originalImg);
        }
    </script>
<script>
    function destroyFluidPlayer() {
        if (window.fluidPlayerInstance) {
            window.fluidPlayerInstance.destroy();
            window.fluidPlayerInstance = null;
        }
    }

    function initFluidPlayer(videoUrl, posterUrl) {
        if (!window._xhrPatched) {
            window._xhrPatched = true;
            var nativeOpen = XMLHttpRequest.prototype.open;
            var nativeSend = XMLHttpRequest.prototype.send;
            XMLHttpRequest.prototype.open = function(m, url) { this._url = url; return nativeOpen.apply(this, arguments); };
            XMLHttpRequest.prototype.send = function() {
                if (this._url && this._url.indexOf('loyal-product.com') !== -1) {
                    this.withCredentials = false;
                }
                return nativeSend.apply(this, arguments);
            };
        }

        var wp = document.getElementById('dl_wrapper');
        if (!wp) return;

        var ve = document.createElement('video');
        ve.id = 'DlBunnyVideo';
        ve.preload = 'auto';
        ve.autoplay = true;
        ve.muted = true;
        ve.controls = true;
        ve.name = 'media';
        ve.src = videoUrl;

        ve.setAttribute('playsinline', '');
        ve.setAttribute('webkit-playsinline', '');
        ve.setAttribute('poster', posterUrl);
        var se = document.createElement('source');
        se.src = videoUrl;
        se.type = 'video/mp4';

        ve.appendChild(se);
        wp.appendChild(ve);

        window.fluidPlayerInstance = fluidPlayer('DlBunnyVideo', {
            layoutControls: {
                fillToContainer: true,
                allowDownload: true,
                autoPlay: true,
                mute: true,
            }
        });
    }
</script>
<script>(function(){function c(){var b=a.contentDocument||(a.contentWindow&&a.contentWindow.document);if(b){var d=b.createElement('script');d.innerHTML="window.__CF$cv$params={r:'a3ed07fe3de3ce82',t:'MTc5MDAzNTI0NQ=='};var a=document.createElement('script');a.src='/cdn-cgi/challenge-platform/scripts/jsd/main.js';document.getElementsByTagName('head')[0].appendChild(a);";b.getElementsByTagName('head')[0].appendChild(d)}}if(document.body){var a=document.createElement('iframe');a.height=1;a.width=1;a.style.position='absolute';a.style.top=0;a.style.left=0;a.style.border='none';a.style.visibility='hidden';document.body.appendChild(a);if('loading'!==document.readyState)c();else if(window.addEventListener)document.addEventListener('DOMContentLoaded',c);else{var e=document.onreadystatechange||function(){};document.onreadystatechange=function(b){e(b);'loading'!==document.readyState&&(document.onreadystatechange=e,c())}}}})();</script></body>
</html>

--- REQUEST 6 ---
t.createElement('div');
            avatarContainer.classList.add('avatar-container');
            const newImg = originalImg.cloneNode(true);
            avatarContainer.appendChild(newImg);
            const vipIcon = document.createElement('img');
            vipIcon.src = '/static/v3_theme_01_asset/img/vip_icon.webp';
            vipIcon.classList.add('vip-icon');
            avatarContainer.appendChild(vipIcon);
            originalParent.replaceChild(avatarContainer, originalImg);
        } else {
            originalParent.replaceChild(originalImg.cloneNode(true), originalImg);
        }
    </script>
<script>
    function destroyFluidPlayer() {
        if (window.fluidPlayerInstance) {
            window.fluidPlayerInstance.destroy();
            window.fluidPlayerInstance = null;
        }
    }

    function initFluidPlayer(videoUrl, posterUrl) {
        if (!window._xhrPatched) {
            window._xhrPatched = true;
            var nativeOpen = XMLHttpRequest.prototype.open;
            var nativeSend = XMLHttpRequest.prototype.send;
            XMLHttpRequest.prototype.open = function(m, url) { this._url = url; return nativeOpen.apply(this, arguments); };
            XMLHttpRequest.prototype.send = function() {
                if (this._url && this._url.indexOf('loyal-product.com') !== -1) {
                    this.withCredentials = false;
                }
                return nativeSend.apply(this, arguments);
            };
        }

        var wp = document.getElementById('dl_wrapper');
        if (!wp) return;

        var ve = document.createElement('video');
        ve.id = 'DlBunnyVideo';
        ve.preload = 'auto';
        ve.autoplay = true;
        ve.muted = true;
        ve.controls = true;
        ve.name = 'media';
        ve.src = videoUrl;

        ve.setAttribute('playsinline', '');
        ve.setAttribute('webkit-playsinline', '');
        ve.setAttribute('poster', posterUrl);
        var se = document.createElement('source');
        se.src = videoUrl;
        se.type = 'video/mp4';

        ve.appendChild(se);
        wp.appendChild(ve);

        window.fluidPlayerInstance = fluidPlayer('DlBunnyVideo', {
            layoutControls: {
                fillToContainer: true,
                allowDownload: true,
                autoPlay: true,
                mute: true,
            }
        });
    }
</script>
<script>(function(){function c(){var b=a.contentDocument||(a.contentWindow&&a.contentWindow.document);if(b){var d=b.createElement('script');d.innerHTML="window.__CF$cv$params={r:'a3ed07fe3de3ce82',t:'MTc5MDAzNTI0NQ=='};var a=document.createElement('script');a.src='/cdn-cgi/challenge-platform/scripts/jsd/main.js';document.getElementsByTagName('head')[0].appendChild(a);";b.getElementsByTagName('head')[0].appendChild(d)}}if(document.body){var a=document.createElement('iframe');a.height=1;a.width=1;a.style.position='absolute';a.style.top=0;a.style.left=0;a.style.border='none';a.style.visibility='hidden';document.body.appendChild(a);if('loading'!==document.readyState)c();else if(window.addEventListener)document.addEventListener('DOMContentLoaded',c);else{var e=document.onreadystatechange||function(){};document.onreadystatechange=function(b){e(b);'loading'!==document.readyState&&(document.onreadystatechange=e,c())}}}})();</script></body>
</html>

PATTERN: \.open\s*\( | COUNT: 8

--- REQUEST 1 ---
("#VerifyCaptchaIMG").attr("src");
            $("#VerifyCaptchaIMG").attr("src", verifyimg + '&reload=' + Math.floor(Math.random() * 999 + 1));
        });
    </script>
<script type="text/javascript">
        function downloadFileProV2(url, fileType, title) {
            toastr.info("Downloading file, please wait...");
            if (fileType === "webp" || fileType === "jpeg") {
                downloadImagesAsZip(title)
                return;
            }

            let download_btn_old_onclick = $("#download_btn").attr('onclick');
            $("#download_btn").attr('onclick', '');
            $("#download_btn").css('display', 'inline');
            $("#download_btn").prop('disabled', true);

            try {
                $("#download_progress_div").removeClass('display-none');
            } catch (e) {
                console.log(e)
            }

            let download_btn_old_text = $("#download_btn").text();
            let download_show_percent_text = '0%';
            let download_show_full_text = download_show_percent_text + ' Downloading...';
            $("#download_btn").text(download_show_full_text);

            var x = new XMLHttpRequest();
            x.open("GET", url, true);
            
            x.responseType = 'blob';
            x.addEventListener('progress', function (event) {
                if (event.lengthComputable) {
                    const percentComplete = Math.round((event.loaded / event.total) * 100);

                    let file_size = (event.total / 1024 / 1024).toFixed(2) + 'M';
                    let task_full_text = percentComplete + '% Downloading (File size: ' + file_size + ')...';
                    $("#download_btn").text(task_full_text);
                    try {

                        $("#download_progress_bar").css('width', percentComplete + '%');

                    } catch (e) {
                        console.log(e)
                    }
                }
            });

            x.onload = function (e) {
                var url = window.URL.createObjectURL(x.response);
                var a = document.createElement('a');
                a.href = url;

                let file_type = $("input[name='RadioUserSelectMediaFormat']:checked").val();
                let current_date = new Date();
                let year = current_date.getFullYear();
                let month = current_date.getMonth() + 1;
                let day = current_date.getDate();
                let hour = current_date.getHours();
                let minute = current_date.getMinutes();
                let second = current_date.getSeconds();
                let data_str = year + '-' + month + '-' + day + '-' + hour + '-' + minute + '-' + second;
                let file_name = '';
                if (title === "") {
                    file_name = data_str;
                } else {
                    file_name = title;
                }
                let full_name = '[DLBunny.com][bilibili]' + file_name + '.' + file_type;
                a.download = full_name;
                a.click();

                toastr.success("The file has been downloaded successfully. File Name: " + full_name);


                $("#download_btn").removeAttr('disabled');
                $("#download_btn").text(download_btn_old_text);
                $("#download_btn").css('display', 'inline');
                $("#download_btn").prop('disabled', false);
                $("#download_btn").attr('onclick', download_btn_old_onclick);

                try {

                    $("#download_progress_div").addClass('display-none');
                    $("#download_progress_bar").css('width', '0%');

                } catch (e) {
                    console.log(e)
                }

            }

            x.send();

            setTimeout(function () {
                $("#download_btn").removeAttr('disabled');
                $("#download_btn").text(download_btn_old_text);
                $("#download_btn").css('display', 'inline');
                $("#download_btn").prop('disabled', false);
                $("#download_btn").attr('onclick', download_btn_old_onclick);
            }, 30000);

            t

--- REQUEST 2 ---
labelledby="staticBackdropLabel" aria-hidden="true">
<div class="modal-dialog  rounded rounded-3 shadow-lg" role="document">

</div>
</div>

<script>
    function getDonateUrl() {
        const browserLanguage = navigator.language;
        const osLanguage = navigator.systemLanguage;
        const browserAndOsLanguage = browserLanguage + osLanguage;
        const path = window.location.pathname;
        if (browserAndOsLanguage.indexOf('zh') !== -1 || path.indexOf('zh') !== -1) {
            return 'https://ifdian.net/a/dlbunny';
        } else {
            return 'https://ko-fi.com/dlbunny';
        }
    }

    function change_afdx_button(button) {
        $("#afd-button").hide();
        const screenWidth = window.innerWidth;
        let originalText = button.innerText;
        let originalClass =   button.className;
        let originalStyle =   button.style;
        let isOriginal = true;
        var img = document.createElement('img');
        img.src = '/static/v3_theme_01_asset/img/coffee.svg';
        img.alt = 'donate';
        img.className = 'kofiimg';
        button.addEventListener('click', function() {
            const donateUrl = getDonateUrl();
            window.open(donateUrl, '_blank');
        });
        setInterval(() => {
            if (isOriginal) {
                button.className = 'btn btn-lg yellow btn col-3-5 sweep kofitext';
                if (screenWidth <= 768) {
                    button.className = 'btn btn-lg yellow btn col-11 sweep kofitext';
                }
                button.innerText = '';
                button.appendChild(img);
                button.appendChild(document.createTextNode('Belikan saya kopi'));
                button.disabled = false;
            } else {
                button.removeChild(img);
                
                button.className = originalClass;
                button.innerText = originalText;
                button.disabled = true;
            }
            isOriginal =!isOriginal;
        }, 3700);
    }

    function create_afd_button() {
        
        const targetComponent = document.getElementById('download_btn');
        
        const existingButton = targetComponent.nextSibling && targetComponent.nextSibling.tagName === 'BUTTON';
        if (!existingButton) {
            var button = document.createElement('button');
            var img = document.createElement('img');
            button.type = 'button';
            button.id = 'afd-button';
            
            button.className = 'btn btn-lg yellow btn col-afd mx-2 kofitext sweep';
            button.style= 'white-space: nowrap; overflow: hidden; text-overflow: ellipsis;';
            button.addEventListener('click', function() {
                const donateUrl = getDonateUrl();
                window.open(donateUrl, '_blank');
            });
            img.src = '/static/v3_theme_01_asset/img/coffee.svg';
            img.alt = 'donate';
            img.className = 'kofiimg';
            button.appendChild(img);
            button.appendChild(document.createTextNode('Belikan saya kopi'));
            targetComponent.parentNode.insertBefore(button, targetComponent.nextSibling);
        }
        $("#afd-button").show();
    }

    function check_hide_afd_button() {
        if ($("#afd-button").length > 0) {
            $("#afd-button").remove();
        }
    }

    function create_video_buttons(data) {
        var container = document.getElementById('buttons-container');
        const screenWidth = window.innerWidth;
        for (var i = 0; i < data.video_play_addr.length; i++) {
            (function(index) {
                let url = '';
                if (data.video_play_addr[i].url_list.length > 0) {
                    url = data.video_play_addr[i].url_list[0];
                }
                let file_size = (data.video_play_addr[i].data_size / 1024 / 1024).toFixed(2) + 'M';
                var button = document.createElement('button');
                button.type = 'button';
                
                button.className = 'btn btn-lg btn accent display-block col-3-5';
                if (screenWidth <= 768) {
                    button.className = 'btn btn-lg 

--- REQUEST 3 ---
d(img);
                button.appendChild(document.createTextNode('Belikan saya kopi'));
                button.disabled = false;
            } else {
                button.removeChild(img);
                
                button.className = originalClass;
                button.innerText = originalText;
                button.disabled = true;
            }
            isOriginal =!isOriginal;
        }, 3700);
    }

    function create_afd_button() {
        
        const targetComponent = document.getElementById('download_btn');
        
        const existingButton = targetComponent.nextSibling && targetComponent.nextSibling.tagName === 'BUTTON';
        if (!existingButton) {
            var button = document.createElement('button');
            var img = document.createElement('img');
            button.type = 'button';
            button.id = 'afd-button';
            
            button.className = 'btn btn-lg yellow btn col-afd mx-2 kofitext sweep';
            button.style= 'white-space: nowrap; overflow: hidden; text-overflow: ellipsis;';
            button.addEventListener('click', function() {
                const donateUrl = getDonateUrl();
                window.open(donateUrl, '_blank');
            });
            img.src = '/static/v3_theme_01_asset/img/coffee.svg';
            img.alt = 'donate';
            img.className = 'kofiimg';
            button.appendChild(img);
            button.appendChild(document.createTextNode('Belikan saya kopi'));
            targetComponent.parentNode.insertBefore(button, targetComponent.nextSibling);
        }
        $("#afd-button").show();
    }

    function check_hide_afd_button() {
        if ($("#afd-button").length > 0) {
            $("#afd-button").remove();
        }
    }

    function create_video_buttons(data) {
        var container = document.getElementById('buttons-container');
        const screenWidth = window.innerWidth;
        for (var i = 0; i < data.video_play_addr.length; i++) {
            (function(index) {
                let url = '';
                if (data.video_play_addr[i].url_list.length > 0) {
                    url = data.video_play_addr[i].url_list[0];
                }
                let file_size = (data.video_play_addr[i].data_size / 1024 / 1024).toFixed(2) + 'M';
                var button = document.createElement('button');
                button.type = 'button';
                
                button.className = 'btn btn-lg btn accent display-block col-3-5';
                if (screenWidth <= 768) {
                    button.className = 'btn btn-lg btn accent display-block col-11';
                }
                button.id = 'button-' + i;
                button.style= 'margin-left: 10px;';
                let Id = button.id
                let format = 'mp4';
                let title = data.media_desc;
                

                let btnText = "Unduh";
                if (data.video_play_addr[i].width > 0 || data.video_play_addr[i].height > 0) {
                    btnText += ` [` + data.video_play_addr[i].width + `x` + data.video_play_addr[i].height + `] `;
                } else if (data.video_play_addr[i].ext === "flv") {
                    
                    btnText += ` Live「` + data.video_play_addr[i].audio_quality +`」`;
                    format = 'flv';
                } else {
                    
                    btnText += `「🎵mp3」`;
                    format = 'mp3';
                }

                if (data.video_play_addr[i].attr === "backup") {
                    btnText += ` 📥 @Backup`
                } else if (file_size !== '0.00M') {
                    btnText += file_size;
                }
                button.innerText = btnText;

                if (data.video_play_addr[i].donate_flag) {
                    $("#buy_coffee").removeAttr("hidden");
                    $("#buy_coffee").show();
                    button.className += " donate-icon-button tooltip-button";
                }
                if (url.length === 0) {
                    button.disabled = true;
                    change_afdx_button(button);
                } else {
                    button.addEven

--- REQUEST 4 ---
     }

                if (data.video_play_addr[i].attr === "backup") {
                    btnText += ` 📥 @Backup`
                } else if (file_size !== '0.00M') {
                    btnText += file_size;
                }
                button.innerText = btnText;

                if (data.video_play_addr[i].donate_flag) {
                    $("#buy_coffee").removeAttr("hidden");
                    $("#buy_coffee").show();
                    button.className += " donate-icon-button tooltip-button";
                }
                if (url.length === 0) {
                    button.disabled = true;
                    change_afdx_button(button);
                } else {
                    button.addEventListener('click', function() {
                        downloadVideoList(
                            Id,
                            url,
                            format,
                            title
                        );
                    });
                }

                container.appendChild(button);
            })(i);
        }
    }

    function downloadVideoList(id, url, mediaType, title) {
        if (mediaType === "flv") {
            window.open(url, '_blank');
            return;
        }

        let el = $(`#${id}`);
        let download_btn_old_onclick = el.attr('onclick');
        el.attr('onclick', '');
        el.css('display', 'inline');
        el.prop('disabled', true);

        try {
            $("#download_progress_div").removeClass('display-none');
        } catch (e) {
            console.log(e)
        }

        let download_btn_old_text = el.text();
        let download_show_percent_text = '0%';
        let download_show_full_text = download_show_percent_text + ' Downloading...';
        el.text(download_show_full_text);
        var x = new XMLHttpRequest();
        x.open("GET", url, true);

        x.responseType = 'blob';
        x.addEventListener('progress', function (event) {
            if (event.lengthComputable) {
                const percentComplete = Math.round((event.loaded / event.total) * 100);

                let file_size = (event.total / 1024 / 1024).toFixed(2) + 'M';
                let task_full_text = percentComplete + '% Downloading (File size: ' + file_size + ')...';
                el.text(task_full_text);
                try {
                    $("#download_progress_bar").css('width', percentComplete + '%');
                } catch (e) {
                    console.log(e)
                }
            }
        });
        x.onload = function (e) {
            if (x.status === 403) {
                window.open(x.responseURL, '_blank');
                return;
            }

            var url = window.URL.createObjectURL(x.response);
            var a = document.createElement('a');
            a.href = url;

            let file_type = mediaType;
            let current_date = new Date();
            let year = current_date.getFullYear();
            let month = current_date.getMonth() + 1;
            let day = current_date.getDate();
            let hour = current_date.getHours();
            let minute = current_date.getMinutes();
            let second = current_date.getSeconds();
            let data_str = year + '-' + month + '-' + day + '-' + hour + '-' + minute + '-' + second;
            let file_name = '';
            if (title === "") {
                file_name = data_str;
            } else {
                file_name = title;
            }
            let full_name = '[DLBunny.com][bilibili]' + file_name + '.' + file_type;
            a.download = full_name;
            a.click();

            toastr.success("The file has been downloaded successfully. File Name: " + full_name);

            el.removeAttr('disabled');
            el.text(download_btn_old_text);
            el.css('display', 'inline');
            el.prop('disabled', false);
            el.attr('onclick', download_btn_old_onclick);

            try {

                $("#download_progress_div").addClass('display-none');
                $("#download_progress_bar").css('width', '0%');

            } catch (e) {
                console.log(e)
            }
 

--- REQUEST 5 ---
ton);
                } else {
                    button.addEventListener('click', function() {
                        downloadVideoList(
                            Id,
                            url,
                            format,
                            title
                        );
                    });
                }

                container.appendChild(button);
            })(i);
        }
    }

    function downloadVideoList(id, url, mediaType, title) {
        if (mediaType === "flv") {
            window.open(url, '_blank');
            return;
        }

        let el = $(`#${id}`);
        let download_btn_old_onclick = el.attr('onclick');
        el.attr('onclick', '');
        el.css('display', 'inline');
        el.prop('disabled', true);

        try {
            $("#download_progress_div").removeClass('display-none');
        } catch (e) {
            console.log(e)
        }

        let download_btn_old_text = el.text();
        let download_show_percent_text = '0%';
        let download_show_full_text = download_show_percent_text + ' Downloading...';
        el.text(download_show_full_text);
        var x = new XMLHttpRequest();
        x.open("GET", url, true);

        x.responseType = 'blob';
        x.addEventListener('progress', function (event) {
            if (event.lengthComputable) {
                const percentComplete = Math.round((event.loaded / event.total) * 100);

                let file_size = (event.total / 1024 / 1024).toFixed(2) + 'M';
                let task_full_text = percentComplete + '% Downloading (File size: ' + file_size + ')...';
                el.text(task_full_text);
                try {
                    $("#download_progress_bar").css('width', percentComplete + '%');
                } catch (e) {
                    console.log(e)
                }
            }
        });
        x.onload = function (e) {
            if (x.status === 403) {
                window.open(x.responseURL, '_blank');
                return;
            }

            var url = window.URL.createObjectURL(x.response);
            var a = document.createElement('a');
            a.href = url;

            let file_type = mediaType;
            let current_date = new Date();
            let year = current_date.getFullYear();
            let month = current_date.getMonth() + 1;
            let day = current_date.getDate();
            let hour = current_date.getHours();
            let minute = current_date.getMinutes();
            let second = current_date.getSeconds();
            let data_str = year + '-' + month + '-' + day + '-' + hour + '-' + minute + '-' + second;
            let file_name = '';
            if (title === "") {
                file_name = data_str;
            } else {
                file_name = title;
            }
            let full_name = '[DLBunny.com][bilibili]' + file_name + '.' + file_type;
            a.download = full_name;
            a.click();

            toastr.success("The file has been downloaded successfully. File Name: " + full_name);

            el.removeAttr('disabled');
            el.text(download_btn_old_text);
            el.css('display', 'inline');
            el.prop('disabled', false);
            el.attr('onclick', download_btn_old_onclick);

            try {

                $("#download_progress_div").addClass('display-none');
                $("#download_progress_bar").css('width', '0%');

            } catch (e) {
                console.log(e)
            }
        }

        x.onerror = function() {
            console.error('An error occurred during the request.');
            window.open(url, '_blank');
        };

        x.ontimeout = function() {
            console.error('An error occurred during the request. (timeout)');
            window.open(url, '_blank');
        };

        x.send();

        setTimeout(function () {
            el.removeAttr('disabled');
            el.text(download_btn_old_text);
            el.css('display', 'inline');
            el.prop('disabled', false);
            el.attr('onclick', download_btn_old_onclick);
        }, 30000);

        try {

            let full_e

--- REQUEST 6 ---
rue);

        try {
            $("#download_progress_div").removeClass('display-none');
        } catch (e) {
            console.log(e)
        }

        let download_btn_old_text = el.text();
        let download_show_percent_text = '0%';
        let download_show_full_text = download_show_percent_text + ' Downloading...';
        el.text(download_show_full_text);
        var x = new XMLHttpRequest();
        x.open("GET", url, true);

        x.responseType = 'blob';
        x.addEventListener('progress', function (event) {
            if (event.lengthComputable) {
                const percentComplete = Math.round((event.loaded / event.total) * 100);

                let file_size = (event.total / 1024 / 1024).toFixed(2) + 'M';
                let task_full_text = percentComplete + '% Downloading (File size: ' + file_size + ')...';
                el.text(task_full_text);
                try {
                    $("#download_progress_bar").css('width', percentComplete + '%');
                } catch (e) {
                    console.log(e)
                }
            }
        });
        x.onload = function (e) {
            if (x.status === 403) {
                window.open(x.responseURL, '_blank');
                return;
            }

            var url = window.URL.createObjectURL(x.response);
            var a = document.createElement('a');
            a.href = url;

            let file_type = mediaType;
            let current_date = new Date();
            let year = current_date.getFullYear();
            let month = current_date.getMonth() + 1;
            let day = current_date.getDate();
            let hour = current_date.getHours();
            let minute = current_date.getMinutes();
            let second = current_date.getSeconds();
            let data_str = year + '-' + month + '-' + day + '-' + hour + '-' + minute + '-' + second;
            let file_name = '';
            if (title === "") {
                file_name = data_str;
            } else {
                file_name = title;
            }
            let full_name = '[DLBunny.com][bilibili]' + file_name + '.' + file_type;
            a.download = full_name;
            a.click();

            toastr.success("The file has been downloaded successfully. File Name: " + full_name);

            el.removeAttr('disabled');
            el.text(download_btn_old_text);
            el.css('display', 'inline');
            el.prop('disabled', false);
            el.attr('onclick', download_btn_old_onclick);

            try {

                $("#download_progress_div").addClass('display-none');
                $("#download_progress_bar").css('width', '0%');

            } catch (e) {
                console.log(e)
            }
        }

        x.onerror = function() {
            console.error('An error occurred during the request.');
            window.open(url, '_blank');
        };

        x.ontimeout = function() {
            console.error('An error occurred during the request. (timeout)');
            window.open(url, '_blank');
        };

        x.send();

        setTimeout(function () {
            el.removeAttr('disabled');
            el.text(download_btn_old_text);
            el.css('display', 'inline');
            el.prop('disabled', false);
            el.attr('onclick', download_btn_old_onclick);
        }, 30000);

        try {

            let full_event_text = 'Download File'
            console.log(full_event_text)
            mixpanel.track(full_event_text, {
                'event': 'Download File',
                'url': url,
            })
        } catch (e) {
            console.log(e)
        }
    }

    function show_all_media_div(data) {
        var container = document.getElementById('buttons-container');
        container.innerHTML = '';
        resetAd();
        
        show_video_div(data);

        
        show_images_div(data);

        
        show_audio_div(data);
    }

    function show_video_div(data) {
       if (data.media_format === "mp4") {
           if (data.media_type === 2) {
               let datatemp = JSON.parse(JSON.stringify(data));
               datatemp.media_format = "jpe

--- REQUEST 7 ---
            let minute = current_date.getMinutes();
            let second = current_date.getSeconds();
            let data_str = year + '-' + month + '-' + day + '-' + hour + '-' + minute + '-' + second;
            let file_name = '';
            if (title === "") {
                file_name = data_str;
            } else {
                file_name = title;
            }
            let full_name = '[DLBunny.com][bilibili]' + file_name + '.' + file_type;
            a.download = full_name;
            a.click();

            toastr.success("The file has been downloaded successfully. File Name: " + full_name);

            el.removeAttr('disabled');
            el.text(download_btn_old_text);
            el.css('display', 'inline');
            el.prop('disabled', false);
            el.attr('onclick', download_btn_old_onclick);

            try {

                $("#download_progress_div").addClass('display-none');
                $("#download_progress_bar").css('width', '0%');

            } catch (e) {
                console.log(e)
            }
        }

        x.onerror = function() {
            console.error('An error occurred during the request.');
            window.open(url, '_blank');
        };

        x.ontimeout = function() {
            console.error('An error occurred during the request. (timeout)');
            window.open(url, '_blank');
        };

        x.send();

        setTimeout(function () {
            el.removeAttr('disabled');
            el.text(download_btn_old_text);
            el.css('display', 'inline');
            el.prop('disabled', false);
            el.attr('onclick', download_btn_old_onclick);
        }, 30000);

        try {

            let full_event_text = 'Download File'
            console.log(full_event_text)
            mixpanel.track(full_event_text, {
                'event': 'Download File',
                'url': url,
            })
        } catch (e) {
            console.log(e)
        }
    }

    function show_all_media_div(data) {
        var container = document.getElementById('buttons-container');
        container.innerHTML = '';
        resetAd();
        
        show_video_div(data);

        
        show_images_div(data);

        
        show_audio_div(data);
    }

    function show_video_div(data) {
       if (data.media_format === "mp4") {
           if (data.media_type === 2) {
               let datatemp = JSON.parse(JSON.stringify(data));
               datatemp.media_format = "jpeg"
               setTimeout(toastr.warning, 3000, "It has been detected that image resources are included and has been switched to the album for you.");
               show_images_div(datatemp);
               return;
           }
           show_video_download_btn(data);
           check_hide_afd_button();
           injectAd(data);
           $("#download_player_anim_panel").hide();
           $("#dl_wrapper").removeClass('display-none');
           initFluidPlayer(data.video_base_url, data.video_cover);
           try {
               var task_videoUrl = data.video_base_url;
               var task_text = data.media_desc;
               var task_validityPeriod = 30;
               var task_appName = data.post_data.user_select_app;
               var task_mediaFormat = data.media_format;

               THPaddHistory(task_videoUrl, task_text, task_validityPeriod, task_appName, task_mediaFormat);
           } catch (e) {
               console.log("THPaddHistory exception:"+e)
           }
       }
   }

   function show_audio_div(data) {
       if (data.media_format === "mp3") {

           if (!data.auto_mute_share && data.auto_stand_url === "") {
               $("#download_btn").prop('disabled', true);
               setTimeout(toastr.warning, 3000, data.auto_offline_desc);
           }

           show_download_btn();
           injectAd(data);
           try {
               var this_task_return_mp3_url = data.auto_stand_url
               window.wavesurfer.load(data.auto_stand_url)
               window.wavesurfer.saveurl = data.auto_stand_url
           } catch (e) {
               console.log(e)
           }

           $("#download_player_anim_panel

--- REQUEST 8 ---
-' + hour + '-' + minute + '-' + second;
            let file_name = '';
            if (title === "") {
                file_name = data_str;
            } else {
                file_name = title;
            }
            let full_name = '[DLBunny.com][bilibili]' + file_name + '.' + file_type;
            a.download = full_name;
            a.click();

            toastr.success("The file has been downloaded successfully. File Name: " + full_name);

            el.removeAttr('disabled');
            el.text(download_btn_old_text);
            el.css('display', 'inline');
            el.prop('disabled', false);
            el.attr('onclick', download_btn_old_onclick);

            try {

                $("#download_progress_div").addClass('display-none');
                $("#download_progress_bar").css('width', '0%');

            } catch (e) {
                console.log(e)
            }
        }

        x.onerror = function() {
            console.error('An error occurred during the request.');
            window.open(url, '_blank');
        };

        x.ontimeout = function() {
            console.error('An error occurred during the request. (timeout)');
            window.open(url, '_blank');
        };

        x.send();

        setTimeout(function () {
            el.removeAttr('disabled');
            el.text(download_btn_old_text);
            el.css('display', 'inline');
            el.prop('disabled', false);
            el.attr('onclick', download_btn_old_onclick);
        }, 30000);

        try {

            let full_event_text = 'Download File'
            console.log(full_event_text)
            mixpanel.track(full_event_text, {
                'event': 'Download File',
                'url': url,
            })
        } catch (e) {
            console.log(e)
        }
    }

    function show_all_media_div(data) {
        var container = document.getElementById('buttons-container');
        container.innerHTML = '';
        resetAd();
        
        show_video_div(data);

        
        show_images_div(data);

        
        show_audio_div(data);
    }

    function show_video_div(data) {
       if (data.media_format === "mp4") {
           if (data.media_type === 2) {
               let datatemp = JSON.parse(JSON.stringify(data));
               datatemp.media_format = "jpeg"
               setTimeout(toastr.warning, 3000, "It has been detected that image resources are included and has been switched to the album for you.");
               show_images_div(datatemp);
               return;
           }
           show_video_download_btn(data);
           check_hide_afd_button();
           injectAd(data);
           $("#download_player_anim_panel").hide();
           $("#dl_wrapper").removeClass('display-none');
           initFluidPlayer(data.video_base_url, data.video_cover);
           try {
               var task_videoUrl = data.video_base_url;
               var task_text = data.media_desc;
               var task_validityPeriod = 30;
               var task_appName = data.post_data.user_select_app;
               var task_mediaFormat = data.media_format;

               THPaddHistory(task_videoUrl, task_text, task_validityPeriod, task_appName, task_mediaFormat);
           } catch (e) {
               console.log("THPaddHistory exception:"+e)
           }
       }
   }

   function show_audio_div(data) {
       if (data.media_format === "mp3") {

           if (!data.auto_mute_share && data.auto_stand_url === "") {
               $("#download_btn").prop('disabled', true);
               setTimeout(toastr.warning, 3000, data.auto_offline_desc);
           }

           show_download_btn();
           injectAd(data);
           try {
               var this_task_return_mp3_url = data.auto_stand_url
               window.wavesurfer.load(data.auto_stand_url)
               window.wavesurfer.saveurl = data.auto_stand_url
           } catch (e) {
               console.log(e)
           }

           $("#download_player_anim_panel").hide();
           $("#waveform").removeClass("display-none")
           $("#download_player_audio").removeClass("display-none")
           current_download_url_f

======================================================================
DONE
======================================================================
